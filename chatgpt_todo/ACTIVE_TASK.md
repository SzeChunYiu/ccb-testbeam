# Active Task

- **Task ID:** `ARU-MC-CS-WORKER-INIT-001`
- **Owner:** hourly Atomic Research Universe audit session
- **Session stamp:** `2026-08-10T180650Z`
- **Protected main after parent merge:** `f5f96951c3f56986769a16cd53ab8e23dee3e287` (PR #1181 deterministic exact-inverse/source-support repair squash-merged after exact-head MC Validation runs `31415757686` and `31415753649` both succeeded).
- **Parent scientific boundary:** #1181 landed `linear_node_pdf_exact_inverse_v1` on `measured_table_support_truncate_v1` with `unit_direct_sampling_v1`; #1178 remains open because repository CI does not compile/execute `geant4/src_patch`, configured-source fault semantics and manifest/runtime closure remain unresolved, and #1179 separately owns source covariance/systematics.
- **Selected atom:** generator configuration/readiness lifecycle `configured tables -> per-instance validated state -> BeamEnergy/SampleThetaCM -> primary event`.
- **Deterministic finding on merged source blob `4a9f3de78ec12147159e2ec9b6fb52bf76f1cbe7`:** `GeneratePrimaryVertex()` still uses `if(event->GetEventID()==0) LoadFiles();`; `BeamEnergy()->EvalELoss()` assumes populated stopping arrays; empty/inconsistent CDF state can become uniform `theta_cm`; required-file open failure uses `exit(0)`; cross-section `sscanf` conversion count is unchecked; there is no explicit idempotent per-instance readiness state.
- **Required state contract:** `UNINITIALIZED -> UNCONFIGURED_UNIFORM | CONFIGURED_READY | FATAL`; configured-source failure must never collapse into the explicit unconfigured uniform proposal.
- **Geant4 lifecycle boundary:** official Geant4 documentation makes event-zero unsuitable as a general worker-local initialization primitive because user actions are per worker in MT and event numbers handled by a worker are not sequential. The actual hibeam_g4 production run-manager/thread mode remains unresolved and must be bound from exact executable/build/runtime provenance before claiming this mechanism affected a historical run.
- **Repository work:** issue #1182 owns the runtime leaf; PR #1183 (`audit/mc-source-readiness-contract`) contains the executable static audit, focused tests, machine-readable result, immutable ARU archive, and coordination updates. The audit branch has been reconciled with `main@f5f96951...` by a normal two-parent merge commit rather than force-pushing or discarding work.
- **Claim state:** #1178 and CL-021 remain GATED for generator-runtime claims. #880/#1053 remain required for historical `PrimaryWeight` carrier/measure interpretation.
- **No promoted result:** no beam ROOT data, production Geant4 run, detector response, ESS, p-value, PID, penetration, timing, energy, pile-up, rate, or detector-performance quantity changed.
- **Next highest-value atom:** implement #1182 per-instance fail-closed readiness on top of the merged exact inverse; then compile and execute hostile source/stopping-table fixtures plus seeded sequential/parallel generator-only closure with manifest binding. After runtime source identity is stable, proceed to #1179 source statistical/systematic nuisance propagation.
- **Status:** `ACTIVE / PARENT_INVERSE_VALIDATED_ON_MAIN / STATIC_READINESS_FALSIFIER_IMPLEMENTED / PR1183_EXACT_HEAD_CI_REQUIRED / ACTUAL_THREAD_MODE_UNRESOLVED / COMPILED_GEANT4_BLOCKED / SOURCE_UNCERTAINTY_BLOCKED / DETECTOR_INFERENCE_BLOCKED`
