# Latest Handoff

## SiPM authorising build receipt now targets the materialized nested core worktree, not only the gitlink

Selected atom `ARU-SIPM-BUILD-SUBMODULE-WORKTREE-IDENTITY-001` is `ACTIVE / PRE-CI` under open parent #977. Branch `audit/sipm-submodule-worktree-identity-v1` starts from exact protected `main@4376a3f88c8e059a5c1a92c020856c98d31f538b`, whose exact `geant4/single_stave/sipm` gitlink is `ccb-sipm-core@3627dc87137a9f33f511a755671414b11853c0a0`.

Merged #1285 substantially strengthened campaign execution provenance by binding source intent, compile-time superproject/core labels, executable SHA-256, CMake cache, selected CMake/C++ compiler binaries and the Geant4 package sentinel. Its own limitation ledger, however, exposed a narrower source-worktree child: `source_identity()` read the superproject gitlink and outer `git status`, but did not establish that `geant4/single_stave/sipm` was actually a separate checked-out Git worktree at that commit.

The deterministic discriminator used Git 2.47.3 and no RNG. A temporary superproject with a real nested core repository and mode-160000 gitlink was clean. After deleting the nested repository and recreating only the empty gitlink directory, outer `git status --porcelain=v1 --untracked-files=all` remained exactly empty. Adding ordinary non-Git content below that path also left outer status empty. Therefore outer-cleanliness is not sufficient evidence that the nested source exists. A wrong nested Git HEAD and dirty tracked nested source do surface as submodule modification, but they do not close the empty/non-Git case.

The adversarial pass sharpened the fix: testing only whether `git -C <core-path> rev-parse ...` succeeds is also insufficient because Git can discover the parent superproject from a non-Git descendant directory. The nested check must require `rev-parse --show-toplevel` to resolve exactly to the configured core path, then require nested `HEAD == superproject gitlink` and inspect nested tracked/untracked status directly. Requiring a literal `.git` directory was rejected because normal submodules may use a `.git` file.

Production commit `ed9902718e0cba3ae195a630ab0b96721e9249fd` implements that semantic check and upgrades the authorising receipt schema to `ccb-single-stave-build-receipt/2`. It serializes `ccb_sipm_core_worktree_head` and `ccb_sipm_core_worktree_clean_at_receipt`, validates their equality/cleanliness, and makes schema-v1 receipts non-authorising under the strengthened contract. It explicitly records `IGNORED_CCB_SIPM_CORE_FILES_NOT_ATTESTED` because ignored nested files are not covered by ordinary status. Focused-test commit `af6932d5a1340f110fff43fb521b1aa291b5b29d` adds six hostile controls: exact nested PASS; empty-dir FAIL with outer-clean witness; non-Git lookalike FAIL with outer-clean witness; HEAD mismatch FAIL; dirty nested FAIL; schema-v1 rejection. Immutable derivation archive is `chatgpt_todo/archive/2026-08-12T073400Z_ARU-SIPM-BUILD-SUBMODULE-WORKTREE-IDENTITY-001.md`.

Four sequential AI votes: build/provenance lead `ACCEPT bounded repair / BLOCK #977 COMPLETE`; adversarial reviewer `REJECT outer-clean/path-exists formulations / ACCEPT semantic top-level+HEAD model`; independent validator `ACCEPT deterministic Git falsifier / REVISE pending exact-head protected CI / BLOCK detector inference`; claims/provenance reviewer `KEEP #977 OPEN/PARTIAL`. Concern `CCB-977-SUBMODULE-WORKTREE-IDENTITY-001` is recorded on #977.

The new material child is `ARU-SIPM-BUILD-IGNORED-SOURCE-INFLUENCE-001`: determine whether ignored/generated files under `ccb-sipm-core` can participate in compilation. Existing residuals remain: mutate-and-restore/time-of-check races, exact compiler/link invocation attestation, runtime loaded-library identity, shared launcher/verifier byte binding, historical pre-#1280 sidecar authenticity, #1072 requested/effective operating-point closure and #1067 measured-electronics authorization/calibration. These prevent #977 from becoming COMPLETE even if this bounded child passes CI.

Repository execution remains pending. The local execution environment could not resolve `github.com`, so there is no local exact-repository pytest claim; only the independent Git fixtures above were executed. Open a draft PR from the current branch and require protected exact-final-head MC Validation before merge. Preserve any CI falsifier and do not weaken the source-worktree contract merely to make the test green.

Concurrent unrelated open work includes #1291 statistics/event-cluster identity, #1290/#1289/#1287/#1286 and #1279 DAQ saturation authorization. Do not absorb those branches. No beam bytes, production Geant4 campaign, measured electronics/SiPM calibration, timing/PID result, rate, ESS, p-value, or detector-performance claim was produced or promoted by this atom.
