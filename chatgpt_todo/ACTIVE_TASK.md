# Active Task

- **Selected atom:** `ARU-TIMING-CFD-NOISE-PHASE-SATURATION-SENSITIVITY-001` is `PARTIAL`, child of #1059/#1063.
- **Parent integration:** selector-identifiability/component-bound-crossing parent merged through non-draft replacement PR #1278 as protected `main@5b7312e8ecabdfbfb9fe2d74a26a4e70352eaec6`. Its exact pre-merge head `d2ba6a37776d14b6fdcd0967c9e724e4752c24aa` passed both PR run `31557640867` and push run `31557638606`. #1059 remains OPEN/PARTIAL.
- **Beam-data blocker:** immutable HRD ROOT bytes are external to GitHub under `/projects/hep/fs10/shared/nnbar/ccb_data/hrd/root` and unavailable here. The checked-in real-data timing output is explicitly `FLAWED_LEGACY_OUTPUT_QUARANTINED`; no new beam metric is inferred from it.
- **Atomic additive law:** for selected local candidate `j`, `F=alpha*max(y)`. Under arbitrary `||delta||_inf<eps`, sufficient persistence radii are `(y[j]-F)/(1+alpha)` for floor eligibility and half the two neighbour-order margins. Each earlier ineligible sample receives the strongest certificate among its currently failed predicates; the exact-selected-index sufficient radius is the minimum across selected eligibility and earlier exclusion. Guarantee is strict and non-authorising.
- **Near-floor discriminator:** `[0,25,49.9,25,0,0,500,1000,500]`, `alpha=.05`, switches under adversarial `+eps` early / `-eps` global at exact `eps*=0.1/1.05=0.09523809523809524 ADC`.
- **Baseline discriminator:** on `[0,20,40,20,0,0,500,1000,500]`, common residual offset gives `m'(b)=m+(1-alpha)b`; selector switches at `b*=10/.95=10.526315789473685 ADC`.
- **Clipping discriminator:** same fixture with `min(y,C)` retargets the unchanged 40-ADC early component when `C<=800 ADC`; tests distinguish `C=801` from `C=800`.
- **Sampling-phase discriminator:** synthetic separated triangular fixture sampled at `n+phi`; examples select indices `10/3/10/9` at `phi=0/.2/.5/.8`. Deterministic 1001-point phase support grid yields `{3:229,9:300,10:472}`. These are fixture-support counts, not a detector probability.
- **Controls:** clean `[0,50,100,50,0]` has 25-ADC sufficient exact-index radius; a `100/100/100` plateau has zero; monotonic fallback has 5 ADC.
- **Implementation:** PR #1277 is retargeted to protected `main` after parent integration. The final child branch contains only the new sensitivity module, seven focused tests, immutable ARU archive, and coordination updates beyond main. No RNG or toy-MC detector claim.
- **Four-role votes:** timing lead `ACCEPT deterministic sensitivity law / BLOCK detector-stability inference`; adversarial DAQ reviewer `ACCEPT estimator counterexamples / BLOCK occurrence/mechanism claims`; independent validation `ACCEPT deterministic oracle / REJECT probabilistic interpretation`; claims/provenance `ACCEPT bounded child / KEEP #1059 OPEN/PARTIAL`.
- **Next physical children:** real-data transition matrix; measured baseline-residual distribution/covariance; actual DAQ clipping/transfer closure; sampling-phase/aperture distribution; held-out truth transfer.
- **Claim boundary:** no beam timing resolution, pile-up/saturation rate or mechanism, WLS response, PID, efficiency, rate, ESS, p-value or detector-performance quantity is regenerated or promoted.
- **Status:** `PARENT_INTEGRATED / DETERMINISTIC_NUISANCE_SENSITIVITY_IMPLEMENTED / CHILD_EXACT_HEAD_CI_REQUIRED / RAW_BEAM_BYTES_BLOCKED / #1059_OPEN_PARTIAL / TIMING_CLAIMS_GATED`.
