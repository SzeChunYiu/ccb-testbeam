# Active Task

- **Selected atom:** `ARU-SIPM-BUILD-SUBMODULE-WORKTREE-IDENTITY-001` under open #977; status `ACTIVE / PRE-CI` on branch `audit/sipm-submodule-worktree-identity-v1`.
- **Exact base:** protected `main@4376a3f88c8e059a5c1a92c020856c98d31f538b`; exact gitlink is `ccb-sipm-core@3627dc87137a9f33f511a755671414b11853c0a0`.
- **Trigger:** merged #1285 bound campaign source intent, compile labels, executable SHA-256, CMake/toolchain and Geant4 sentinels, but explicitly left `ARU-SIPM-BUILD-SUBMODULE-WORKTREE-IDENTITY-001`: outer superproject cleanliness does not prove that the gitlink path is a materialized independent nested Git worktree at the pinned commit.
- **Exact contract:** `gitlink(H_root, core_path)=H_link`, nested `top_level(core)=core_path`, nested `HEAD=H_link`, and nested tracked/untracked status `CLEAN` for an authorising receipt, in addition to the existing outer-tree and executable/toolchain closure.
- **Executed deterministic falsifier:** Git 2.47.3 temporary fixture. Proper nested worktree => outer status `''`; delete nested repo and leave only empty gitlink directory => outer status still `''`; add ordinary non-Git descendant content => outer status still `''`. Wrong nested Git HEAD or dirty tracked source does surface as ` M geant4/single_stave/sipm`. Therefore `outer clean + gitlink` is not sufficient for nested source identity.
- **Adversarial semantic correction:** `git -C <empty descendant> rev-parse --show-toplevel` can discover the parent superproject, so merely requiring `rev-parse` success is insufficient. The repair requires the reported Git top-level to equal the exact configured core path.
- **Implementation:** production commit `ed9902718e0cba3ae195a630ab0b96721e9249fd` upgrades build-receipt schema to `ccb-single-stave-build-receipt/2`, records/validates exact nested worktree HEAD and cleanliness, and makes receipt v1 non-authorising. Focused-test commit `af6932d5a1340f110fff43fb521b1aa291b5b29d` adds six hostile controls; archive commit `72e46786b5e182ad0efcbbaa08a03500867f6a1c` records the atomic derivation and role votes.
- **Review votes:** build/provenance lead `ACCEPT repair / BLOCK #977 COMPLETE`; adversarial reviewer `REJECT outer-clean/path-exists hypotheses / ACCEPT semantic top-level+HEAD model`; independent validator `ACCEPT deterministic Git falsifier / REVISE pending exact-head CI / BLOCK detector inference`; claims/provenance reviewer `KEEP #977 OPEN/PARTIAL`.
- **Residual child introduced by the repair:** `ARU-SIPM-BUILD-IGNORED-SOURCE-INFLUENCE-001`, because ordinary `git status --untracked-files=all` does not expose ignored nested files; determine whether any ignored/generated core files can enter compilation before strengthening that boundary. Existing compiler/link invocation, runtime loaded-library, shared launcher/verifier-byte, historical-output, #1072 and #1067 leaves remain.
- **Concurrent unrelated work:** open #1291 statistics/event-cluster identity, #1290/#1289/#1287/#1286, and draft #1279 DAQ saturation work exist; do not absorb or overwrite them. Current main advanced independently during this session through the #1077/#1288 integration.
- **Next gate:** open a draft bounded PR from this branch and require exact-final-head protected MC Validation. No merge or PASS claim until required contexts are green. If CI falsifies the implementation, preserve the failure and repair without weakening the source-worktree contract.
- **Scientific boundary:** no beam bytes, production Geant4 population, measured electronics/SiPM calibration, DATA↔MC result, timing/PID metric, pile-up efficiency, rate, ESS, p-value, or detector-performance quantity was generated or promoted.
