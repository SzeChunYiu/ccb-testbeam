# Active Task

- **Task ID:** `ARU-MC-G4-LOADER-INITIAL-CWD-001`.
- **Owner:** hourly Atomic Research Universe audit session.
- **Protected main / base:** `c485d96583df91e90669e402670a3fa102643495`, the squash merge of #1213 after both exact-head `test` contexts on `85894ad0123ee56dc18da6cc86e0340f9eabb312` completed successfully.
- **Branch / PR:** `audit/geant4-loader-cwd`; draft PR to be opened from this branch. CL-021 remains gated.
- **Parent scientific universe:** distinguish a stable process-visible current working directory from the historical cwd at `execve`, because `geant4/setup_and_run.sh` invokes `./hibeam_g4 -c krakow.config -m run_krakow.mac output_krakow.root` with relative config/macro/output spellings.
- **Implemented bounded contract:** compose PASS/digest-valid runtime and argv receipts for the same `(pid,starttime_ticks,exe_link)`; observe `/proc/<pid>/cwd` twice; independently open the cwd directory object twice and require equal `(st_dev,st_ino,st_mode)`; then recheck process starttime and executable link. Receipt scope is `STABLE_CURRENT_WORKING_DIRECTORY_OBJECT_OBSERVATION_ONLY`.
- **Key falsifier:** a real Linux child launched with `cwd=initial` then called `chdir(later)` after exec. The attestor correctly observed `later`, directly falsifying the hypothesis that a later `/proc/<pid>/cwd` observation is immutable launch/exec cwd evidence.
- **Local deterministic validation:** Python 3.13.5 / Linux 6.18.35 x86_64 / no RNG. `python -m pytest -q tests/test_geant4_loader_cwd_attestation.py` -> `9 passed in 1.17 s`; `python -m py_compile tools/audit/geant4_loader_cwd_attestation.py tests/test_geant4_loader_cwd_attestation.py` -> PASS. Local ruff was unavailable; attempted installation failed because the package index could not be resolved, so repository CI is required and no local ruff PASS is claimed.
- **Exact committed identities:** tool Git blob `bb71a692732c3f6730b52704bd51ec9506cff7ac`, 10,190 bytes, SHA-256 `02ed0bb6cd4f53a7e72e59f0147e06eee72e7a7518c0d8de11aa62b856f5e1be`; test Git blob `c1f9ffb43856aa17435e931194a94a1df68486c2`, 10,285 bytes, SHA-256 `5d77e26e8233d8693af19d93bbf4bff4b6fbe45a68f8f91d19e95ec6862ffa28`.
- **Implementation commits:** `475d0f886b0257b1cfd905e798254a07ec8a8dd8` (attestor), `c0131e9cc7740303505554266b207fa42567bf70` (hostile tests), `7acb18fa686a2456093c61087491c2a7ec2a114d` (curated CI), `b8d66da03d194040d3bd44bc386aa83098841604` (immutable ARU archive).
- **Four-role state:** runtime/physics lead `ACCEPT current-state primitive / BLOCK initial-cwd provenance`; adversarial Linux/filesystem reviewer `ACCEPT fail-closed transition checks / BLOCK complete path resolution`; independent validation reviewer `ACCEPT deterministic oracle / BLOCK physics inference`; claims/provenance reviewer `ACCEPT provenance refinement / BLOCK CL-021 promotion`.
- **Children:** `ARU-MC-G4-LOADER-INITIAL-CWD-EXEC-BOUNDARY-001`, `ARU-MC-G4-LOADER-FS-NAMESPACE-001`, `ARU-MC-G4-RELATIVE-INPUT-CONSUMPTION-001`, `ARU-MC-G4-OUTPUT-PATH-CREATION-001`, plus the remaining loader cache/config, token/hwcaps, preload/audit, linker/static-input, late-dlopen, wrapper, immutable-consumption, runtime-manifest, source/stopping, event-weight and detector-response atoms.
- **Next gate:** open a draft PR and require fresh exact-final-head MC Validation for both push and pull-request contexts, curated ruff, full non-integration pytest, diagnostics upload and enforcement, plus base-freshness against the then-current protected main. Merge only if every required exact-head context is green.
- **Status:** `PARTIAL / IMPLEMENTED_ON_BRANCH / EXACT_HEAD_CI_REQUIRED / INITIAL_EXEC_CWD_BLOCKED / RELATIVE_INPUT_CONSUMPTION_BLOCKED / PHYSICS_INFERENCE_BLOCKED`.
