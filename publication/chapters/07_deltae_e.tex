\section{Amplitude \texorpdfstring{$\Delta E$--$E$}{Delta E--E} and the segmentation limit}

\subsection{Observable definition}

The supervisor-defined beam-data analogue of a range-telescope observable is
\[
\Delta E_{\rm data}=A(B2),\qquad
E_{\rm data}=A(B4)+A(B6)+A(B8),
\]
where \(A\) is a baseline-subtracted ADC amplitude. These axes are response proxies, not calibrated energies.

A data-matched MC view should use the same four physical readout positions,
\[
\Delta E_{\rm MC,4}=E_{\rm dep}(B2),\qquad
E_{\rm MC,4}=E_{\rm dep}(B4)+E_{\rm dep}(B6)+E_{\rm dep}(B8),
\]
while a full-segmentation truth view must sum each unique downstream physical layer exactly once. A B2-versus-B4 plot is a two-channel response diagnostic and must not be called \(\Delta E\)--\(E\).

\subsection{Sparse-segmentation mechanism}

Alternating readout positions can miss the layer in which the stopping-power rise becomes largest. A change in entry energy, angle or passive material can move the Bragg-rise region between sampled and unsampled layers and thereby alter the apparent sparse \(\Delta E\)--\(E\) topology. The publication test is therefore a controlled comparison of full physical layers and the exact data-matched readout mask, with readout phase treated as a nuisance until the hardware map is source-bound.

\subsection{Cycle-3 status of the current production bundle}

\begin{publicationhold}
The numerical bundle remains \textbf{not} publication-authorising. The producer was repaired: (i) beam-data input now uses the authorising 8-by-16 raw schema; (ii) threshold-censoring order is corrected; (iii) physical-layer and readout namespaces are kept distinct. Corrected-run evidence is recorded per claim in the ledger. Gate status as of 2026-08-14: (iv) the channel-to-layer map remains a documented simulation contract, not a hardware-source-bound drawing; (v) the saturation and transfer threshold remains a software contract without hardware source-binding (recorded as a preserved blocker; no hardware identity is invented); (vi) MC production provenance is closed: the regenerated 100{,}000-event campaign carries a complete production receipt in a machine-readable manifest. The quarantine on the OLD bundle stays; no promotion to validated status is authorized until all gates pass and a final figure package is delivered.
\end{publicationhold}

The quarantined plots are kept in a separate gated figure area to preserve provenance without allowing accidental use in the paper.

\GatedFigure{figures/gated/fig07_data_deltaE_E_per_sample.png}{0.92}{Quarantined current beam-data amplitude $\Delta E$--$E$ plot.}{Gated by the Cycle-3 audit; do not cite as a final result.}

The final Figure~7/8 production must start from an event-level table that contains all four reconstructed channel amplitudes before analysis-threshold censoring, then apply declared threshold scans; import the source-bound readout map; keep physical-layer and readout namespaces distinct; and use a production MC whose source measure and run provenance are authorising.

\subsection{Species-resolved topology and penetration deliverables}

The supervisor-requested species and penetration set is delivered from the
corrected event tables of the regenerated-campaign producer. Species-resolved
\(\Delta E\)--\(E\) panels (proton truth, deuteron truth, colour-coded
proton+deuteron, and all particles) are produced for Sample~I and Sample~II in
both the full downstream-sum and the data-matched four-layer definitions.
Penetration-depth distributions (\(L_{\rm stop}\): deepest physical layer with
a deposit above a stated threshold, with a threshold-stability scan) are
produced per species and per sample, together with the fraction of events
reaching each downstream readout position (B4/B6/B8) and the beam-data
Sample~I versus Sample~II stopping-layer overlay from the producer's
threshold-pass logic. Beam-data axes remain amplitude proxies (not calibrated
energy); B2 saturation is shown only as the analysis-contract flag, with no
numeric hardware level asserted.

The MC panels are Geant4 deposited-energy truth views from a 2{,}000{,}000-event
scale-up of the regenerated campaign
(recorded in a machine-readable manifest; identical
sampler, geometry and runtime stack as the authorising 100{,}000-event
campaign). Because both campaigns run the default engine seed over the same
sequential stream, the scale-up replicates the authorising campaign as a
verified sequential prefix: the leading B-stack-entrance event rows are
identical event-by-event, so the added 1.9 million events extend the same
random stream (the leading 100{,}000 are duplicated, not independently
redrawn) and only densify the same truth topology. The set is descriptive truth topology, not a detector-response
prediction: readout thresholds, light collection and digitisation are not in
the loop, so no numeric agreement with the amplitude-domain data figures is
claimed or implied. Beam-data figures remain gated under the same Cycle-3
gates as Figure~7/8; the MC truth views are descriptive simulations and promote no quarantined number.

\PaperFigure{figures/final/618_mc_truth_full_I.pdf}{0.95}{MC truth $\Delta E$--$E$, Sample I, full downstream sum; proton/deuteron/combined/all panels (descriptive simulation).}{Species-resolved truth topology from the 2M regenerated campaign; descriptive MC only, not a detector-response prediction.}
\PaperFigure{figures/final/618_mc_truth_full_II.pdf}{0.95}{MC truth $\Delta E$--$E$, Sample II, full downstream sum; same panel structure (descriptive simulation).}{Same provenance as the Sample~I figure; unweighted event counts shown on panels.}
\PaperFigure{figures/final/618_mc_truth_4layer_I.pdf}{0.95}{MC truth $\Delta E$--$E$, Sample I, data-matched four-layer definition (descriptive simulation).}{Uses only the same physical readout positions as the data (B2/B4/B6/B8).}
\PaperFigure{figures/final/618_mc_truth_4layer_II.pdf}{0.95}{MC truth $\Delta E$--$E$, Sample II, data-matched four-layer definition (descriptive simulation).}{Same definition as the Sample~I data-matched view.}
\PaperFigure{figures/final/618_penetration_I.pdf}{0.95}{MC penetration depth $L_{\rm stop}$, Sample I: proton, deuteron, shape-normalised overlay, all particles (descriptive simulation).}{Threshold-stability scan and reaching fractions are provided as supplementary material.}
\PaperFigure{figures/final/618_penetration_II.pdf}{0.95}{MC penetration depth $L_{\rm stop}$, Sample II, same panel structure (descriptive simulation).}{Deepest-layer threshold and its stability variants documented in the bundle tables.}
\GatedFigure{figures/gated/618_data_amplitude_I_II.png}{0.95}{Beam-data amplitude analogue $\Delta E$--$E$, Samples I and II on identical axes (gated).}{Amplitude proxies, not calibrated energy; B2 saturation shown as the analysis-contract flag only; Cycle-3 gates apply as for Figure~7/8.}
\GatedFigure{figures/gated/618_data_penetration_I_II.png}{0.95}{Beam-data stopping-layer overlay, Sample I versus II, amplitude domain (gated).}{Uses the producer's threshold-pass logic; no new ADC cut is introduced here.}
