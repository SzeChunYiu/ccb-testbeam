# Active Task

- **Task ID:** `ARU-SIPM-PREWINDOW-DARK-HISTORY-001`, bounded simulator-law repair validated; parent #1096 remains open/PARTIAL.
- **Owner:** hourly Atomic Research Universe audit session.
- **Protected main at selection:** `0a2e46355c5c5060bbbcad9347b525857fdf40da`, squash merge of #1224. The integrated pre-repair gitlink points to `ccb-sipm-core@2027b06e0fb47b26da1b89e95b6901a5f8e6c200`.
- **Atomic contract:** under the explicit-history representation, homogeneous dark primaries satisfy `N_dark ~ Poisson(R*(window_end_ns-history_start_ns)*1e-9)` and conditional times are uniform on `[history_start_ns,window_end_ns]`. Waveform sampling remains restricted to `[window_start_ns,window_end_ns]`.
- **Confirmed pre-repair contradiction:** the scheduler accepted candidates from `history_start_ns`, while DCR exposure/time support still began at `window_start_ns`. At testbeam defaults (`history=-200 ns`, `window_start=-20 ns`, `window_end=250 ns`, `R=500 kHz`) the omitted model interval is 180 ns, `E[N_missing]=0.09`, `P(N_missing>=1)=0.08606881472877181`, and 40% of the history-inclusive primary measure was absent. These are simulator-law quantities, not detector measurements.
- **Core repair:** `ccb-sipm-core` PR #4 changed the DCR duration and uniform-time lower bounds to `history_start_ns`, added `tests/test_dark_history.cc`, wired it into CMake, and added minimal CMake/build/ctest GitHub Actions coverage. PR #4 squash-merged as `ccb-sipm-core@f009b0d6d0ccdcd4f54ef9be62b793934a56e518`.
- **Discriminating regression:** fixed run seed `0x5a17d4c3`; 4000 event IDs each for a 300-ns history-inclusive support (`mu=1.5` at 5 MHz) and a 100-ns window-only control (`mu=0.5`), so the expected aggregate candidate-count ratio is exactly 3. Acceptance requires `2 < ratio < 4`, at least one accepted pre-window dark avalanche, and no dark avalanche outside declared support. Correlated noise and waveform generation are disabled; 10,000 cells and negligible recovery time minimize collision/recovery confounding.
- **Executed validation:** PR-head Core CI run `31517705149`, job `93866882189`, on exact head `22d5122a28cebd94e5981c9b31eeb74689978c41` passed checkout/configure/build/test. Exact merged-main push Core CI run `31517804015`, job `93867201585`, on `f009b0d6d0ccdcd4f54ef9be62b793934a56e518` independently passed configure/build/test. A local clone attempt failed before source acquisition because the container could not resolve `github.com`; no local compile result is claimed.
- **Testbeam integration branch:** `fix/sipm-dcr-history-integration`, based on exact `main@0a2e463...`. Gitlink update commit `4186b9a3c569fc33d8b46180de66883b3b4c646f` points `geant4/single_stave/sipm` to `f009b0d...`. Execution archive commit `55200f643943522f8c21bbb8f74d6797f46576dd` records equations, mechanisms, experiments, failures, role votes, and cross-scale limits.
- **Four-role state:** SiPM/electronics lead `ACCEPT bounded DCR-history repair / REVISE #1096`; adversarial stochastic-process reviewer `ACCEPT explicit-history support / BLOCK sufficiency/full-history claim`; independent validation reviewer `ACCEPT software/statistical oracle / BLOCK detector inference`; claims/provenance reviewer `ACCEPT child VALIDATED / BLOCK #1096 COMPLETE and claim promotion`.
- **Eliminated hypotheses:** a silent physical DCR gate at `window_start_ns` is unsupported by the encoded model; sample-only generation plus reweighting cannot restore zero-probability pre-window histories or their nonlinear recovery/correlated-noise state. A validated sufficient-state initialization remains a surviving alternative.
- **Cross-scale boundary:** dark-primary support is repaired, but admitted early avalanches are not yet guaranteed to propagate through every recorded sample because the impulse kernel is currently only as long as the waveform sample window. No beam data, measured DCR, production MC, detector baseline/timing/pile-up/PID/rate/ESS/p-value, or detector-performance quantity was regenerated or promoted.
- **Surviving children:** `ARU-SIPM-PREWINDOW-KERNEL-COVERAGE-001`, `ARU-SIPM-HISTORY-HORIZON-CONVERGENCE-001`, and a possible `ARU-SIPM-SUFFICIENT-STATE-001` if explicit prehistory is replaced by a boundary-state model.
- **Next highest-value atom:** `ARU-SIPM-PREWINDOW-KERNEL-COVERAGE-001`. With defaults, the sample window spans 270 ns while an avalanche at `history_start=-200 ns` can be 450 ns old at the final sample; current `make_impulse_kernel(n_samples,dt)` therefore cannot represent nonzero impulse support over the full admitted history-to-sample lag unless an explicit finite-support/truncation rule is validated.
- **Status:** `ACTIVE / DCR_HISTORY_CHILD_VALIDATED / CORE_PR4_MERGED_AND_EXACT_MAIN_CI_GREEN / TESTBEAM_GITLINK_UPDATE_STAGED / #1096_PARTIAL / KERNEL_COVERAGE_CHILD_OPEN / DETECTOR_CLAIMS_GATED`.
