# Scientific Review Backlog

| Task ID | Priority | Status | Scientific impact | Dependencies | Acceptance criteria |
|---|---:|---|---|---|---|
| AUD-G4-001 | P0 | COMPLETE | Geant4 MT RNG validation complete | GPU node runs, all validators pass | ✅ 1T=48T same-seed events exact equal; 1,170,091 photons exact equal; optical yield=178.3 PE confirmed; different seeds produce independent streams |
| AUD-G4-002 | P0 | COMPLETE | Optical yield ~178 PE/event validated | GPU node runs with full provenance | ✅ Mean=178.3 PE, RSE=0.48% across 4 seeds; all metadata hashes, geometry, optical tables recorded; 500 events per seed |
| AUD-G4-003 | P0 | COMPLETE | Prevent environment-specific Geant4 build products from masquerading as source or validation artifacts | PR #888 file inventory; current `main` | ✅ All 66 tracked files under `geant4/single_stave/build/` removed; `geant4/**/build/` ignored; Git-index regression added and synthetically validated; PR #888/#889 source files preserved; cleanup present on remote `main` as `c7cdd653c5fef08b1e70cb33db9c574f7e7e0de9` |
| AUD-G4-004 | P0 | COMPLETE | Prevent a synthetic self-test from bypassing the committed PSTAR reference table | PR #890 script and static reference CSV | ✅ Default resolves below repository root; missing selected reference fails closed; inline fallback removed; path/SHA-256/row count reported; focused regression `3 passed`; validation record and visual evidence committed to `main` |
| AUD-G4-005 | P0 | PARTIAL | Establish an accepted stopping-power closure rather than a local-deposition proxy | Clean Geant4 build/runtime; particle/material/cut/physics-list provenance; immutable outputs | Compare a validated projectile-energy-loss observable or `G4EmCalculator::ComputeTotalDEDX` against the reference; quantify generated-secondary escape and energy evolution; report proton and approximate deuteron comparisons separately; retain commands, versions, seeds, event counts, input/output hashes, uncertainty, and diagnostic plots |
| AUD-G4-006 | P0 | COMPLETE | Prevent unsupported PSTAR endpoint reuse from masquerading as an in-domain comparison | `AUD-G4-004`; committed PSTAR table; stopping-power diagnostic | ✅ Finite positive proton-equivalent lookup required; exact endpoints accepted; below/above-range lookups rejected rather than clamped; deuteron `E/2` lookup reported; CSV includes lookup and table bounds; focused regression `7 passed`; Markdown/JSON/SVG evidence on `main` |
| AUD-G4-007 | P0 | COMPLETE | Prevent malformed or reordered PSTAR rows from silently altering the numerical reference | `AUD-G4-004`; `AUD-G4-006`; static PSTAR table | ✅ Required columns and every data row validated; finite physical values and strictly increasing energies required; malformed references return CLI status 2 without numerical PASS; exact old defect reproduced; focused suite `14 passed`; Markdown/JSON/SVG evidence on `main` |
| AUD-G4-008 | P0 | COMPLETE | Prevent quenched visible-energy output from masquerading as raw PSTAR agreement | `AUD-G4-004`; stopping-power script; raw/quenched column contract | ✅ Quenched-only input fails by default; explicit proxy mode is labelled and non-accepting; mixed semantics are rejected; CSV records basis and comparability; focused suite `18 passed`; Markdown/JSON/SVG evidence on `main` |
| AUD-G4-009 | P0 | COMPLETE | Provide a reusable fail-closed parser for stopping-power simulation event tables | `AUD-G4-008`; simulation event CSV contract | ✅ Shared parser validates every noncomment row, exact particle/energy/deposit/track semantics, finite physical values, one alias per quantity, raw/quenched consistency, normalization, and exact input SHA-256; standalone regression `17 passed`; Markdown/JSON/SVG evidence on `main` |
| AUD-G4-010 | P0 | COMPLETE | Prevent the canonical PSTAR CLI from bypassing the strict simulation-table parser | `AUD-G4-009`; `compare_stopping_power.py` | ✅ Canonical CLI delegates to the shared parser, malformed and ambiguous rows return status 2 before numerical PASS, normalized rows feed aggregation, output records input hash/bytes/validated-row count/validator version, focused combined suite `35 passed`, and integration Markdown/JSON/SVG evidence is on `main` |
| AUD-G4-011 | P0 | PARTIAL | Validate exact real exported event-table bytes before interpreting a stopping-power ratio | `AUD-G4-010`; real Geant4 export; immutable provenance | Run the integrated CLI on exact real exports; retain table path, byte size, SHA-256, row count, basis, code commit, command, environment, particle/energy coverage, output hash, and any rejection. This input-validation task does not replace `AUD-G4-005` accepted physics closure. |
| AUD-G4-012 | P0 | COMPLETE | Prevent an internally inconsistent PSTAR total column from silently biasing every ratio | `AUD-G4-007`; exact committed PSTAR CSV | ✅ Exact-decimal shared parser v1.1.0 enforces `total = electronic + nuclear`; canonical comparison invokes it directly; inconsistent direct CLI input returns status 2 with no result CSV or numerical PASS; valid outputs record reference hash/bytes/rows/version/identity; combined focused suite `42 passed`; Markdown/JSON/SVG evidence is on `main` |
| AUD-G4-013 | P0 | COMPLETE | Prevent distinct configured energies from being silently pooled into an undocumented 0.1 MeV bin before PSTAR lookup | `AUD-G4-010`; `AUD-G4-012`; canonical comparison | ✅ Exact old blob reproduced `1.01,1.04 -> 1.025 MeV` and failed the new regression; current aggregation groups by exact validated numeric energy, records `EXACT_CONFIGURED_ENERGY` in rows/CSV/CLI, preserves grouping for equivalent numeric tokens, focused suite `19 passed`, and Markdown/JSON/SVG evidence is on `main` |
| AUD-G4-014 | P0 | COMPLETE | Prevent proton PSTAR at `E_d/2` from masquerading as a direct, accepting deuteron reference | `AUD-G4-006`; canonical comparison; NIST PSTAR scope | ✅ Deuteron input fails closed by default; explicit proxy mode is labelled `VELOCITY_SCALED_PROTON_PROXY` and cannot set `within_tolerance`; results/CSV record reference and physics comparability; mixed proton/deuteron output is non-accepting; self-test is proton-only; focused suite `9 passed`; Markdown/JSON/SVG evidence is on `main` |
| AUD-G4-015 | P0 | COMPLETE | Prevent a point estimate inside an arbitrary tolerance from masquerading as accepted stopping-power agreement without uncertainty | `AUD-G4-014`; canonical comparison; NIST TN 1297 reporting principles | ✅ Numerical point-estimate state remains visible, but `uncertainty_method=NOT_EVALUATED`, `within_tolerance=false`, CLI status is nonzero, no PASS is printed, self-test remains explicitly arithmetic-only, exact old blob fails the new four-test regression, focused suite `19 passed`, and Markdown/JSON/SVG evidence is on `main` |
| AUD-G4-016 | P0 | COMPLETE | Prevent report serialization from erasing exact configured-energy identity and other floating-point results | `AUD-G4-013`; canonical comparison output | ✅ Exact old blob wrote distinct `1.0000001` and `1.0000002` MeV groups as identical CSV token `1` and terminal label `1.00`; current CSV uses finite Python round-trip representations for every float, records `PYTHON_REPR_ROUND_TRIP`, prints exact energy tokens, rejects nonfinite output, focused regression `3 passed`, old-blob negative control `2 failed`, and Markdown/JSON/SVG evidence is on `main` |
| AUD-G4-017 | P0 | COMPLETE | Make stopping-power reports independently reconstructable instead of exposing only derived values and status | `AUD-G4-016`; canonical comparison output | ✅ Every result/CSV row records summed deposited energy, summed track length, material density, tolerance percentage, and `RATIO_OF_SUMS_TRACK_LENGTH_WEIGHTED`; the central proxy and point-estimate gate can be reconstructed exactly; exact old blob fails the two-test regression, focused suite `5 passed`, and Markdown/JSON/SVG evidence is on `main` |
| AUD-G4-018 | P0 | COMPLETE | Prevent CSV row order from changing stopping-power sufficient statistics through non-associative binary64 accumulation | `AUD-G4-017`; canonical comparison aggregation | ✅ Exact old blob produced different deposit sums and proxies for one reordered event multiset; current groups use `math.fsum` for deposit and track totals, report `MATH_FSUM_PER_GROUP`, focused suite `8 passed`, exact old negative control `2 failed, 1 passed`, and Markdown/JSON/SVG evidence is on `main` |
| AUD-G4-019 | P0 | COMPLETE | Prevent simulation rows from being paired with byte size/SHA-256 measured from a later replacement of the input path | `AUD-G4-010`; canonical simulation parser | ✅ Validator v1.2.0 reads exact bytes once, parses and derives size/hash from the same byte string, records `SINGLE_READ_EXACT_BYTES`, converts invalid UTF-8 to controlled status-2 failure, passes `19` focused tests, and has former-algorithm negative control plus Markdown/JSON/SVG evidence on `main` |
| AUD-G4-020 | P0 | COMPLETE | Prevent an unweighted arithmetic mean across distinct energy points from masquerading as a combined stopping-power closure estimate | `AUD-G4-015`; canonical comparison report | ✅ Canonical reporter removes `statistics.mean`, preserves individual exact-energy points, emits only descriptive min/max bounds labelled `no combined estimate`, prints and records `NO_CROSS_ENERGY_COMBINATION_WITHOUT_UNCERTAINTY_MODEL`, stores the policy in every result/CSV row, source audit returns `VALIDATED`, focused combined regression `6 passed`, and Markdown/JSON/SVG evidence is on `main` |
| AUD-G4-021 | P0 | COMPLETE | Prevent a derived stopping-power report from overwriting validated simulation/reference inputs or leaving a truncated final artifact | `AUD-G4-010`; canonical comparison output path | ✅ Resolved-path and existing-file alias checks reject simulation/reference targets before input reads; reports serialize to a unique same-directory temporary file, flush/fsync, measure bytes/SHA-256, and publish with `os.replace`; failures remove temporary files and preserve a previous final report; direct CLI/symlink/injected-failure regressions plus report compatibility returned `12 passed`; AST audit is `VALIDATED`; Markdown/JSON/SVG evidence is on `main`. |
| AUD-I885-001 | P0 | COMPLETE | Prevent partial campaign coverage and repeated-seed fits from masquerading as validated proton/deuteron calibration | Issue #885 manifest, partial result bundle, plotter | ✅ Independent validator, focused tests, corrected coverage summary, calibration quarantine, and Markdown/JSON/SVG acceptance evidence are on remote `main` |
| AUD-I885-002 | P0 | COMPLETE | Regenerate issue #885 fit diagnostics using independent energy points and test whether a global line is supported | `AUD-I885-001`; committed 14-file partial summary CSV | ✅ One seed-averaged point per energy; separate file/energy counts; minimum three energies; propagated within-file and between-seed uncertainty; residual dof, chi-square, p-value, covariance and range retained; deuteron skipped; proton lines rejected; accepted `fits` empty; focused regression `6 passed`; JSON/CSV/SVG/Markdown evidence on remote `main` |
| AUD-I885-003 | P0 | PARTIAL | Establish a scientifically accepted proton/deuteron response model and calibration after adequate coverage | Complete issue #885 campaign or preregistered validation subset; `AUD-I885-002`; real-data closure | Complete manifest-derived coverage; preregister model family/range before validation data; compare linear, saturation/quenching, and monotonic alternatives with held-out or newly simulated energies; quantify seed/run/systematic uncertainty and coverage; require residual diagnostics and data/MC closure before publishing calibration constants |
| AUD-DELTAE-001 | P0 | PARTIAL | Correct A-002 event-table cardinality and rerun stopping/ΔE–E outputs | `AUD-AMP-009`; `AUD-AMP-010`; `AUD-DELTAE-002`; exact A-002 table/evidence | ✅ Composite-key and signed-polarity transformation already validated. A new content-addressed strict runner now requires expected input SHA-256 and clean expected commit, verifies input identity before/after processing, validates unique event rows and stopping totals, records code/command/runtime provenance in JSON/CSV/SVG, and transactionally publishes/restores the complete bundle; focused regression `9 passed`; Markdown/JSON/SVG evidence is on `main`. Completion still requires accepted A-002 convention/polarity evidence, the exact production rerun, uncertainties, and scientific closure. |
| AUD-DELTAE-002 | P0 | PARTIAL | Prevent polarity-blind absolute ADC conversion from creating false stopping deposits | `AUD-AMP-009`, `AUD-AMP-010`; exact A-002 table and polarity evidence | Absolute input requires hash-bound positive/negative pulse polarity; conversion is signed (`amplitude-baseline` or `baseline-amplitude`), opposite-polarity/nonfinite rows fail closed, result metadata records polarity and formula, focused tests pass, and real outputs are regenerated only after accepted evidence |
| AUD-DELTAE-006 | P0 | COMPLETE | Prevent Parquet rows from being paired with byte count/SHA-256 measured from a later replacement of the input path | canonical `deltaE_E.py`; immutable input provenance | ✅ `.parquet` and `.pq` are read once as bytes, parsed through `io.BytesIO`, and reuse the retained bytes for manifest provenance; result/manifest contracts publish `SINGLE_READ_EXACT_BYTES`; exact former source is `FLAWED` with seven findings, current source is `VALIDATED` with zero findings, focused regression `7 passed`, and Markdown/JSON/SVG evidence is on `main` |
| AUD-DELTAE-007 | P0 | COMPLETE | Prevent malformed, missing-value, or nonfinite present signal cells from masquerading as absent zero-deposit layers | canonical DeltaE front door/core; event-table integrity | ✅ Present data `amp_B2/B4/B6/B8` and every present MC `edep_B*` must coerce to finite numeric before derived quantities; only wholly absent supported downstream columns zero-fill; result/manifest publish both policies; focused regression `19 passed`; Markdown/JSON/SVG evidence is on `main` |
| AUD-AMP-009 | P0 | PARTIAL | Prevent fabricated or stale supporting-artifact digests from authorizing amplitude subtraction | Exact A-002 table bytes; exact schema/producer/pedestal evidence bytes | Validator must resolve references beneath a controlled root, reject missing/escaping/mutated artifacts, and require measured SHA-256 equality; auditor must accept physics use only from a verified evidence map; focused regression and full supported checks pass; real A-002 map records exact table and evidence hashes before outputs are regenerated |
| AUD-AMP-010 | P0 | PARTIAL | Prevent decorative or nonexistent evidence fragments from masquerading as claim-level provenance | `AUD-AMP-009`; exact supporting evidence artifact | Whole-file evidence references remain allowed; any fragment must be canonical `#L<start>` or `#L<start>-L<end>`, resolve within the byte-verified artifact, and record verified bounds; malformed, reversed, empty, or out-of-range fragments fail closed; real A-002 evidence must cite the exact supporting lines or explicitly cite the whole file |
| AUD-CI-002 | P0 | COMPLETE | Restore current-main amplitude-audit tests and invalid-baseline failure accounting | PR #884; Actions run `29993563323` | ✅ Stale warning assertion synchronized; non-NET `BASELINE_DATA_INVALID` convention rows counted even without hash-bound evidence; MC Validation CI succeeded; PR #884 squash-merged to remote `main` as `4f857f508160bbbe059d936866b426a45788c9bd` |
| AUD-LEDGER-001 | P0 | PARTIAL | Prevent shifted CSV fields from masquerading as canonical claim status, truth type, source, or uncertainty metadata | `docs/claim_ledger.csv`; source reports/scripts/data for every claim | ✅ Current schema record reports 26/26 exact-width rows with zero width issues. Claim-level scientific review remains partial: exact width does not validate values, sources, uncertainties, statuses, or downstream wording. Completion requires source-backed review of every row and downstream WIKI/claim/link/figure checks. |
| AUD-LEDGER-002 | P0 | COMPLETE | Prevent the claim-ledger schema validator from overwriting the ledger or publishing partial validation artifacts | `AUD-LEDGER-001`; canonical schema validator | ✅ Validator v1.1.0 rejects input/output and JSON/SVG aliases, atomically publishes through same-directory temporary files with flush/fsync/`os.replace`, preserves previous outputs on injected failure, cleans temporary files, and returns controlled status 2; focused combined suite `19 passed`; Markdown/JSON/SVG evidence and immutable handoff are on `main`. |
| AUD-P04P-001 | P0 | PARTIAL | Establish a robust duplicate-readout harm-veto model selection rather than a point-gate winner at the uncertainty boundary | P04p result/report/script/config/manifest; independent runs and cross-stave transfer | ✅ Audit reproduces the reported point-gate GBT winner and shows that its 95% coverage interval crosses 0.50; a lower-bound sensitivity gate changes the first eligible method to MLP; `CL-015` is `GATED`; focused regression `6 passed`; Markdown/JSON/SVG evidence is on `main`. Completion requires a preregistered uncertainty-aware coverage rule, multiplicity policy, independent holdout/new runs, B4/B6/B8 transfer validation, and immutable rerun provenance before a production winner is named. |
| AUD-P07E-001 | P0 | PARTIAL | Prevent strong synthetic pseudo-saturation recovery from authorizing a real-data correction contradicted by external duplicate closure | P07e report/result/script/config/manifest; content-addressed producer; cross-stave and independent-run validation | ✅ Audit separates pseudo-saturation ML res68 `0.036691` from external held-out duplicate closure; external ML `0.176358 [0.173043,0.180602]` is worse than raw `0.120794 [0.117004,0.125364]`; `CL-016` is exact-width and `GATED`; focused regression `4 passed`; Markdown/JSON/SVG evidence is on `main`. Completion requires recovered or cleanly rerun producer-byte provenance plus preregistered cross-stave/new-run validation before any production correction. |
| AUD-MC-003 | P0 | PARTIAL | Replace issue #880 fail-open producer with a strict content-addressed weighted rerun | `AUD-MC-001`; `AUD-MC-002`; exact ROOT input | ✅ Reusable strict estimators and a strict rerun entry point require one finite nonnegative weight per event, explicit directions and denominators, before/after ROOT SHA-256 identity, clean git provenance, protected atomic JSON, and overwrite gating; focused tests `17 passed`; Markdown/JSON/SVG evidence is on `main`. Completion requires the exact one-million-event rerun, weighted uncertainty, weight-tail stability, regenerated plots, and data/MC closure. |
| AUD-REPO-001 | P1 | ACTIVE | Build complete repository-wide audit coverage | Claimed by concurrent LUNARC session | Every study/code/data/simulation/figure/table/wiki area appears in `MASTER_INDEX.md` with a stable ID and state |
| AUD-WIKI-001 | P1 | PARTIAL | Verify and cross-link wiki claims | `WIKI.md`, claim ledger, source reports | C12 status corrected in authoritative ledger; remaining wiki claims inventoried and mapped to evidence, code, data, plots, and limitations; public WIKI wording synchronized |
| AUD-WIKI-002 | P1 | COMPLETE | Prevent the public WIKI front door from contradicting its status legend and canonical claim ledger | `WIKI.md`; `docs/claim_ledger.csv`; `AUD-WIKI-001` | ✅ All three MV4 raw labels are `VALIDATED`; τeff truth type is `data + MC self-consistent`; uncertainty incompleteness is explicit; bound rows `CL-007`/`CL-011` align to 43 columns; validator v1.1.0 returns `VALIDATED` with zero issues; focused tests `5 passed`; 16 internal WIKI targets validated; Markdown/JSON/SVG evidence is on `main` |
| AUD-ANOM-001 | P1 | PARTIAL | Revalidate MV6 C12 anomaly transfer from MC to data | Matched data/MC morphology definitions and event-level provenance | Execute the preregistered contract in `docs/validation/C12_DATA_MC_CLOSURE_SPEC.md`: report counts and Wilson intervals, frozen cross-domain selection, morphology/rate closure, MC purity and efficiency, stability/falsifier tests, hashes, JSON summary, and required plots; make no empirical C12 claim without independent data evidence |
| AUD-CI-001 | P1 | COMPLETE | PR #868 lint gate resolved | Lint fixes at `7992aa31`, CI run `29861328983` | ✅ E501 fixes applied, CI passed (pytest+ruff); Geant4 runtime validation remains as BLK-G4-001 |

## Selection rule

Choose the highest-priority dependency-resolved task not already active or complete. Do not duplicate work owned by another active session.
