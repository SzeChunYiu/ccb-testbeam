# Active Task

- **Task ID:** `ARU-MC-G4-LOADER-INITIAL-CWD-EXEC-BOUNDARY-001` (#1214), with front-door adoption child `CWD-EXEC-002` and downstream filesystem/input/output children.
- **Owner:** hourly Atomic Research Universe audit session.
- **Protected main / base:** `859903ada4a856c998b2bc79298cd4a26c2cb447`, the squash merge of #1215 (`ARU-MC-G4-LOADER-INITIAL-CWD-001`).
- **Branch:** `audit/geant4-exec-cwd-boundary`. CL-021 remains gated.
- **Parent scientific universe:** the HIBEAM front door uses relative `krakow.config`, `run_krakow.mac`, and `output_krakow.root`; a later stable `/proc/<pid>/cwd` observation is current state, not immutable historical exec-cwd evidence.
- **Implemented bounded contract:** parent selects cwd and direct ELF target objects; isolated `python -I -S` helper starts in the selected cwd, receives target environment out-of-band, records cwd/target/PID/starttime/exact argv, marks its control fd close-on-exec, and immediately calls fd-based `execve`. Parent requires pipe EOF plus identical PID/starttime and target inode through `/proc/<pid>/exe`. Runtime binding additionally requires exact `(pid,starttime_ticks,exe_link)` and executable-object identity with a digest-valid runtime receipt.
- **Equations/invariants:** `D_parent == D_pre` by `(st_dev,st_ino)`; `X_parent == X_pre == X_post` by `(dev_major,dev_minor,inode)`; `PID_pre == PID_post`; `starttime_pre == starttime_post`. This binds only `CWD_exec`; full `Resolved_t(p)=Resolve(CWD_t,Root_t,MountNS_t,p)` remains open.
- **Mechanisms eliminated:** later procfs cwd as historical launch evidence; parent cwd substitution; wrapper/shebang equivalence; target-environment contamination of helper startup; cwd-only promotion to exact relative-input bytes.
- **Executed local validation:** Python 3.13.5 / Linux 6.18.35 x86_64 / glibc 2.41 / no RNG. Authoring bytes: six consecutive focused runs, each `10 passed` (1.09–1.22 s), plus `py_compile`. Exact committed bytes reconstructed from Git blob identity: three consecutive `10 passed` (1.25, 1.15, 1.17 s), plus `py_compile`. Local ruff unavailable; no local ruff PASS claimed.
- **Exact committed identities:** tool 32,497 bytes, SHA-256 `b278d4def1e2ba4ed07e46f9964d6a8c499e16c0b854968263fed06e3614f42c`, Git blob `7b12c5db3e616f0bc2c314ced864ce042c86aab5`; tests 11,876 bytes, SHA-256 `b0ef626cc20045d9f2a022a24827510b2c0639681317e7b7`, Git blob `2ee73530c96d489791ab540acaec5e6c1531713d`. The repository-write surface omitted only the final LF relative to the authoring copies; that difference was measured and the exact committed bytes were rerun.
- **Immutable record:** `chatgpt_todo/archive/2026-08-11T122000Z_ARU-MC-G4-LOADER-INITIAL-CWD-EXEC-BOUNDARY-001.md`.
- **Four-role state:** runtime/physics lead `ACCEPT bounded direct-exec mechanism / BLOCK production provenance`; adversarial Linux/filesystem reviewer `ACCEPT direct-ELF fail-closed route / BLOCK wrappers and namespace claims`; independent validation reviewer `ACCEPT deterministic local oracle / REVISE until exact-head CI`; claims/provenance reviewer `ACCEPT provenance refinement / BLOCK CL-021 promotion`.
- **Open children:** `CWD-EXEC-002` real `setup_and_run.sh` adoption + real HIBEAM composition; `ARU-MC-G4-LOADER-FS-NAMESPACE-001`; `ARU-MC-G4-RELATIVE-INPUT-CONSUMPTION-001`; `ARU-MC-G4-OUTPUT-PATH-CREATION-001`; helper-source runtime consumption if needed; then remaining loader cache/config, token/hwcaps, preload/audit, linker/static-input, late-dlopen, wrapper/descendant, immutable-consumption, runtime-manifest, source/stopping, event-weight and detector-response atoms.
- **Next gate:** add the new tool/test to exact-head MC Validation, create a draft PR, require curated ruff + full non-integration pytest + diagnostics/enforcement on the final head and recheck base freshness immediately before any ready/merge transition. Even a green PR closes only the bounded mechanism, not production HIBEAM provenance.
- **Status:** `PARTIAL / IMPLEMENTED_ON_BRANCH / LOCAL_COMMITTED_BYTES_VALIDATED / EXACT_HEAD_CI_REQUIRED / FRONTDOOR_ADOPTION_BLOCKED / FS_NAMESPACE_BLOCKED / RELATIVE_INPUT_CONSUMPTION_BLOCKED / PHYSICS_INFERENCE_BLOCKED`.