# Latest Handoff

## SiPM campaign source intent survives two adversarial corrections; exact-head CI required

Selected atom `ARU-SIPM-CAMPAIGN-MANIFEST-CORE-SHA-SOURCE-001` remains `ACTIVE / PRE-CI` in draft PR #1284 under open parent #977. The branch started from protected `main@4abbf9112ad7933b4f0ec5b927f2d0f358ad08be`, which pins `geant4/single_stave/sipm` to exact `ccb-sipm-core@3627dc87137a9f33f511a755671414b11853c0a0`. Base main-push MC Validation run `31565791869` is confirmed `SUCCESS`.

The core source-binding design is unchanged: canonical `ccb-sipm-campaign-intent/1` bytes derive the expected core from the recorded superproject commit, hash every selected points grid, freeze base CLI/event-count/thread intent, copy the manifest digest into every Slurm argv, verify manifest/grid/source binding before simulation, and require the produced compile-bound sidecar core SHA to equal campaign intent before point provenance is written. The first adversarial correction replaced a merely descriptive `SUPERPROJECT_GITLINK:...` source label with semantic `git ls-tree <recorded_commit> geant4/single_stave/sipm` verification.

A second cross-atom review found a source/output contradiction in the launcher. `generate_points.py` explicitly supports environment re-gridding, but the first PR implementation generated into tracked `geant4/single_stave/slurm/grids/` and then called `require_clean_worktree()`. Any non-default `CCB_GRID_*` choice therefore changed tracked `points_*.csv` before manifest creation and failed the provenance gate. A deterministic temporary-Git fixture reproduced the exact failure: after a tracked grid edit, `git status --porcelain=v1 --untracked-files=all` returned ` M geant4/single_stave/slurm/grids/points_test.csv`, making the old clean-worktree predicate fail closed.

The corrected launcher now treats the committed generator as source and generated grids as campaign artifacts. It writes exact campaign-specific grids to `${CCB_CAMPASSIGN_OUTDIR}/grids`, requires the campaign output directory to resolve outside the repository, hashes those exact generated bytes into the manifest, submits those same files, and points the analyzer at the same frozen grid directory. This preserves the strong all-files clean-source gate without forbidding scientifically intended grid variation. A new fake-`sbatch` integration regression uses the actual launcher with `CCB_GRID_PDE_SCALE="0.95 1.05"`, requires successful campaign setup, verifies the manifest's SHA-256 equals the generated external grid bytes, and requires repository `git status` to be unchanged before/after.

The second correction is archived separately at `chatgpt_todo/archive/2026-08-12T060500Z_ARU-SIPM-CAMPAIGN-MANIFEST-CORE-SHA-SOURCE-001_GRID-OUTPUT-CORRECTION.md`; the original derivation and first adversarial addendum remain immutable. The four sequential votes are now: detector-response/provenance lead `ACCEPT source/output separation / REVISE pending CI / BLOCK #977 COMPLETE`; adversarial reviewer `REJECT relaxing clean-source gate / ACCEPT external content-bound grids`; independent validator `ACCEPT deterministic discriminator / REVISE pending protected CI / BLOCK detector inference`; claims/provenance reviewer `KEEP #977 and #1067 OPEN/PARTIAL`.

Material residuals remain. Source intent and campaign-grid hashes do not prove which `ccb_stave_sim` binary/toolchain produced an output. A stale build may still disagree with the manifest superproject commit. Direct/manual `sipm_sensitivity.py --expected-core-sha` remains operator-controlled unless separately manifest-bound. The Slurm job still invokes helper/verifier bytes from a shared checkout rather than a content-addressed execution bundle. Historical pre-#1280 sidecars remain unauthenticated. Retain `ARU-SIPM-CAMPAIGN-POINT-MANIFEST-CONSUMER-001`, `ARU-SIPM-RUN-METADATA-BINARY-BUILD-MANIFEST-001`, `ARU-SIPM-CAMPAIGN-SUPERPROJECT-BUILD-CLOSURE-001`, `ARU-SIPM-CAMPAIGN-LAUNCHER-BYTE-BINDING-001`, `ARU-ELEC-IMPULSE-HISTORICAL-OUTPUT-AUDIT-001`, #1072 and #1067.

Draft PR #1284 is the only intended integration vehicle for this atom; unrelated draft PR #1279 remains DAQ saturation work and must not be absorbed. The prior exact-head CI result became obsolete after the second correction. Require both push and pull-request MC Validation on the new final head before marking ready or merging. No beam bytes, production Geant4 campaign, measured electronics calibration, timing/PID result, rate, ESS, p-value, or detector-performance claim was produced or promoted.
