# Active Task

- **Task ID:** `ARU-MC-G4-OVERLAY-PROVENANCE-001` / concern `PROV-G4-OVERLAY-001`
- **Owner:** hourly Atomic Research Universe audit session
- **Protected main inspected:** `774eda1b1180098c7e00757db312ede41491094b`, after validated coordination PR #1197.
- **Parent dependency:** #1182 / `ARU-MC-CS-COMPILED-PROVENANCE-001`; #1178, #1179, #1058 and CL-021 remain open/gated.
- **Selected atomic contradiction:** the reviewed `geant4/src_patch/patch_scatter.py` installs the approved `ScatteringGenerator.hh/.cc` bytes into a pinned external `hibeam_g4` checkout. Unless upstream already contains identical bytes, that correct overlay makes `git status` non-clean. Therefore the prior shorthand “exact upstream commit/tree + clean state + reviewed installed pair” is over-constrained and can reject the only intended patched state.
- **Required input contract:** exact external `HEAD` commit and `HEAD^{tree}` supplied by an approved run specification; repository-reviewed payload bytes at `geant4/src_patch/ScatteringGenerator.hh/.cc`; external work tree rooted at the exact pinned repository.
- **Preferred state contract:** baseline `HEAD/tree` must match exactly; Git index must remain clean; no untracked paths are allowed; every visible work-tree delta must be an unstaged modification of only `include/ScatteringGenerator.hh` or `src/ScatteringGenerator.cc`; both external paths must be regular non-symlink files byte-identical to the reviewed payload. A baseline with no delta is allowed only if both upstream files already equal the reviewed bytes.
- **Implementation branch:** `audit/geant4-build-frontdoor-provenance` adds `tools/audit/validate_geant4_external_overlay.py`, focused hostile Git-state tests, and includes both files in the curated ruff lane. Exact-head CI is required before merge.
- **Executed logical falsifiers encoded as tests:** intended two-file overlay; already-equivalent clean upstream; interrupted one-file deployment; extra tracked mutation; untracked file; staged overlay; clean-but-wrong source bytes; wrong expected commit/tree.
- **Scientific boundary:** this is pre-build source identity only. It does not compile/link/run Geant4, does not freeze staged input bytes against later mutation, and does not bind compiler/Geant4/VGM/CMake/executable/run-manager/thread/seed/event-count/output provenance.
- **Repository coordination:** stale PR #1195 was closed without merge because its diff reintroduced both the event-zero readiness gate and finite `pull_request.paths`, mechanisms already rejected and repaired on protected main.
- **Next child after local overlay closure:** content-bound staged-input/build/run provenance: exact input hashes at consumption, immutable or reverified source/input snapshot at build/run boundaries, compiler/Geant4/VGM/CMake/executable identity, run-manager/thread mode, random engine/seeds/event count/model IDs, compiled hostile source/stopping controls, and output manifest identity.
- **Status:** `ACTIVE / OVERLAY_CONTRACT_IMPLEMENTED_ON_BRANCH / EXACT_HEAD_CI_REQUIRED / COMPILED_GEANT4_BLOCKED / INPUT_CONSUMPTION_IDENTITY_BLOCKED / RUNTIME_MANIFEST_BLOCKED / DETECTOR_INFERENCE_BLOCKED`
