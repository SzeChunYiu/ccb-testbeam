# Active Task

- **Selected atom:** `ARU-ELEC-IMPULSE-DIGEST-RUNTIME-BINDING-001` is `VALIDATED` at bounded software/provenance-object level in `ccb-sipm-core`; testbeam integration is the current gate. Parent #1067 remains `OPEN/PARTIAL`.
- **Protected testbeam base inspected:** `main@594bea0807e53d5f3e55a2b2e29bd85f82aa1f3e`. Its live SiPM gitlink is `ccb-sipm-core@f0258f5020ba9c8b6b44b284bfcafaeb27528a2c`; #1087 removed the EventAction empty-arrival bypass, so every sensor now traverses the SiPM simulator even without photon arrivals.
- **Governance correction:** #1087's squash message carried historical text `fixes #1067`, which auto-closed P0 #1067 despite explicit PARTIAL coordination and unresolved children. This session reopened #1067 and added #1067 as a second live regression witness to open governance issue #1218. Issue closed/open state is not a physics-completion oracle.
- **Core predecessor:** `ccb-sipm-core@8ecf6037de9f14cc073f5ed99299a8e78a5fadb3` validated canonical SHA-256 identities for parsed sampled impulses and effective numerical kernels, but explicitly left same-runtime-object binding open.
- **Runtime-object contract:** `N_out=floor((window_end-window_start)/dt)+1`, `N_hist=ceil(max(0,window_start-history_start)/dt)`, `N_kernel=N_out+N_hist`. A waveform-producing simulator constructs one private history-complete peak-normalised kernel object `K`; convolution consumes `K` by const reference and metadata records `CanonicalEffectiveKernelHash(dt,K)`. Caller-supplied effective hashes are overwritten. With `generate_waveform=false`, no consumed runtime kernel exists and the effective hash is empty.
- **Waveform law preserved:** `V_i=sum_a A_a h(t_i-t_a)` for causal `t_i>=t_a`, using the existing continuous fractional-delay interpolation. This atom changes provenance/object lifetime, not the numerical interpolation law.
- **Equivalence collapse:** positive global source-amplitude scale is intentionally invisible to the effective hash after peak normalisation; source/sample identities remain distinct. Shape, `dt`, and exact history-complete kernel length are identity-bearing.
- **Core branch/PR:** `audit/impulse-digest-runtime-binding`, core PR #12. Final exact PR head `e50ead854430a41c02bf40c1bf1db8955a9fe10a`.
- **Initial CI contradiction preserved:** run `31530401219` / job `93908776119` configured and built, and the new runtime-binding test passed, but two predecessor tests failed because they still required an empty effective hash. Those stale oracles were repaired to require exact numerical identity while preserving `CUSTOM_UNVALIDATED` authority state; controls were not weakened.
- **Final Core execution:** exact-head Core CI `31530628915` / job `93909520107`, Ubuntu 24.04 / GCC 13.3.0, configure+build SUCCESS, CTest `7/7` passed, `0` failed, total 0.25 s. Tests cover dark history, history-complete kernel, measured-impulse fail-closed, provenance quarantine, canonical digest and new runtime binding.
- **Core merge:** PR #12 squash-merged with expected-head guard as exact core main `66cc250f837be5bd30f8bec9bd67b8ec9ca9e7ed`. Merge message explicitly does not close #1067.
- **Testbeam integration branch:** `audit/impulse-runtime-binding-integration` was created from exact protected `594bea...`. First bounded commit changed only the SiPM gitlink `f0258f5... -> 66cc250...`; archive/coordination changes follow. Protected testbeam CI and final current-main ancestry remain mandatory before merge.
- **Stale PR handling:** existing draft #1233 integrates only predecessor canonicalization (`8ecf6037...`) from stale base `cf3106...`. It is not the preferred integration path; close it as superseded once the new descendant integration PR is open.
- **Scientific boundary:** exact runtime-kernel identity is provenance, not measured calibration. Exact external source bytes, parser binding, units/polarity/baseline/time-zero/normalisation/resampling validation, positive typed `CUSTOM_UNVALIDATED -> MEASURED` authorization, historical-output audit, and physical history-horizon convergence remain unresolved. No detector/public claim is promoted.
- **Four-role votes:** electronics/calibration lead `ACCEPT runtime-object binding / REVISE measured-calibration authority`; adversarial reviewer `ACCEPT fail-closed same-object identity / REJECT caller-hash authority`; independent validation reviewer `ACCEPT deterministic C++ oracle / BLOCK detector inference`; claims/provenance reviewer `ACCEPT bounded provenance advance / BLOCK #1067 COMPLETE and measured-electronics/public claims`.
- **Archive:** `chatgpt_todo/archive/2026-08-11T200100Z_ARU-ELEC-IMPULSE-DIGEST-RUNTIME-BINDING-001.md`.
- **Next highest-value scientific child after integration:** `ARU-ELEC-IMPULSE-SOURCE-BYTE-BINDING-001` — bind exact external calibration bytes and parser contract to the canonical sampled numerical identity. If a real CCB calibration artifact is unavailable, record that exact blocker and keep `MEASURED` authorization blocked.
- **Status:** `CORE_RUNTIME_BINDING_VALIDATED_AND_MERGED / TESTBEAM_INTEGRATION_ACTIVE / #1067_OPEN_PARTIAL / #1218_OPEN_SECOND_REGRESSION_WITNESS / SOURCE_BYTES_AND_CALIBRATION_OPEN / #1096_HISTORY_HORIZON_OPEN / DETECTOR_CLAIMS_GATED`.
