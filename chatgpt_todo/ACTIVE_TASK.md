# Active Task

- **Selected atom:** `ARU-TIMING-CFD-PEAK-SELECTION-IDENTIFIABILITY-001` is `PARTIAL`, child of #1059/#1063. The first-local-peak selector is now treated as an explicit, non-authorising hypothesis rather than an implicit detector-truth component label.
- **Protected main inspected:** branch created from exact protected `main@75b80839042a367e54743401cc2d11cfab6d4c3b`; required status context is `test`. Current main at the last protection inspection remained that SHA.
- **Parent dependency:** current protected main still lacks the component-bound crossing repair from draft #1259 even though #1259 exact head `458506cff29daa9fb8a80a7656ebd73bff180b53` had successful pull-request and push MC Validation runs. #1274 therefore carries that bounded dependency plus the new selector diagnostics; #1259 must remain unmerged until #1274 is validated/supersedes it cleanly.
- **Atomic selector law:** with `A_g=max(y)` and default `alpha=0.05`, interior `j` is eligible when `y[j]>=y[j-1]`, `y[j]>=y[j+1]`, and `y[j]>=alpha*A_g`; select the earliest eligible `j`, else fall back to `argmax(y)`. This is a global-height floor, not topographic prominence.
- **Identifiability margin:** `m_j=A_j-alpha*A_g`. With fixed global-max identity and bounded amplitude perturbations, `|m_j| > eps_j + alpha*eps_g` is sufficient to prevent the floor decision flipping. Neighbour ordering and global-max identity require separate margins; plateaus have zero uniqueness margin under `>=`.
- **Deterministic falsifiers:** a 51-ADC peak between 50-ADC neighbours is selected when the late global peak is 1000 ADC; changing an early peak from 49.9 to 50.1 ADC moves selected index from the late peak to the early peak; a 100/100/100 plateau creates three eligible selector samples; monotonic boundary maxima silently fall back to the global selector.
- **Implementation:** draft PR #1274, branch `audit/cfd-selector-identifiability-v1`, adds named selector profile `first_local_peak_global_fraction_floor_v1`, state `HYPOTHESIS_UNVALIDATED_COMPONENT_IDENTITY`, `authorising_component_identity=false`, detailed selector diagnostics, component-bound crossing semantics from #1259, and ten deterministic tests.
- **Implementation provenance:** code commit `5cf030d2e50e326903f76b077a7234fbc31ba8a0`; implementation/test head `2d8740501d2e3227506458813a6007173310ba0c`; pre-coordination source blob `197ab80571823f6aa13ede1a20b1efc1d9c07b53`; archive commit `6dc182bb280979b9ca62637f0f3c235caeeef2e4`.
- **Execution boundary:** local sparse clone failed before checkout because the execution container could not resolve `github.com`, so there is no local pytest PASS claim. PR-triggered MC Validation run `31550836290` was first observed queued on pre-coordination head; the final coordination head requires fresh exact-head CI before readiness/merge.
- **Stable concern:** `CCB-1059-PEAK-SELECTOR-IDENTIFIABILITY-001` added to existing #1059; no duplicate issue opened. #1059 remains OPEN/PARTIAL.
- **Four-role votes:** timing lead `ACCEPT explicit profile/diagnostics / BLOCK detector-truth validation`; adversarial waveform reviewer `REVISE robustness/prominence interpretation / BLOCK authorising component identity`; independent validation reviewer `ACCEPT deterministic diagnosis / BLOCK timing-resolution inference`; claims/provenance reviewer `ACCEPT bounded fail-closed state / KEEP #1059 OPEN`.
- **Archive:** `chatgpt_todo/archive/2026-08-12T003900Z_ARU-TIMING-CFD-PEAK-SELECTION-IDENTIFIABILITY-001.md`.
- **Next highest-value child:** `ARU-TIMING-CFD-REALDATA-TRANSITION-001` if immutable beam bytes are available; otherwise `ARU-TIMING-CFD-NOISE-PHASE-SATURATION-SENSITIVITY-001` is the strongest valid fixture-level discriminator.
- **Other children:** `ARU-TIMING-CFD-OVERLAP-BASIN-001`, `ARU-TIMING-CFD-TRUTH-TRANSFER-001`, plus report/figure/claim regeneration for consumers of `first_local_peak`.
- **Claim boundary:** the legacy real-data CFD report remains FLAWED/QUARANTINED. No beam timing resolution, pile-up mechanism/rate, WLS response, PID metric, efficiency, rate, ESS, p-value or detector-performance claim is promoted.
- **Status:** `SELECTOR_PROFILE_EXPLICIT / IDENTIFIABILITY_COUNTEREXAMPLES_CONFIRMED_ANALYTICALLY / FINAL_HEAD_CI_PENDING / #1059_OPEN / TIMING_CLAIMS_GATED`.
