# Active Task

- **Selected atom:** `ARU-SIPM-CAMPAIGN-MANIFEST-CORE-SHA-SOURCE-001` remains `ACTIVE / PRE-CI` in draft PR #1284; parent #977 and cross-dependency #1067 remain `OPEN/PARTIAL`.
- **Exact base:** branch started from protected `main@4abbf9112ad7933b4f0ec5b927f2d0f358ad08be`; the exact SiPM gitlink at that commit is `ccb-sipm-core@3627dc87137a9f33f511a755671414b11853c0a0`. Base main-push MC Validation run `31565791869` completed `SUCCESS`.
- **Source-binding invariant:** for campaign intent `I`, `H_expected(I)=H_link(I)`; each orchestrated job requires `H_meta=H_expected`. Canonical manifest bytes and every selected points-grid are SHA-256 bound. The literal manifest digest is copied into Slurm argv, so post-submission manifest/grid mutation fails before simulation.
- **First adversarial correction:** a `SUPERPROJECT_GITLINK:...` source-label string alone was insufficient. The verifier now resolves the manifest's recorded `superproject_commit` with `git ls-tree <commit> geant4/single_stave/sipm` and requires the returned gitlink SHA to equal `expected_core.commit`.
- **Second adversarial correction:** the first launcher generated grids directly into the tracked repository and then required a completely clean worktree. Any documented `CCB_GRID_*` re-grid therefore dirtied tracked `points_*.csv` before manifest creation and failed closed. Campaign-specific grids now live at `${CCB_CAMPASSIGN_OUTDIR}/grids`, outside the repository, while the committed generator remains source-bound and exact generated grid bytes remain manifest-bound. Repository-local campaign output is rejected to preserve the strong clean-source boundary.
- **Deterministic falsifier:** a temporary Git fixture with a tracked `points_test.csv` was re-gridded; `git status --porcelain=v1 --untracked-files=all` returned ` M geant4/single_stave/slurm/grids/points_test.csv`, so the pre-repair clean-worktree predicate returned `FAIL_CLOSED`. This is software/provenance evidence only.
- **Regression added:** fake-`sbatch` integration control runs the actual campaign launcher with `CCB_GRID_PDE_SCALE="0.95 1.05"`, requires successful submission setup, checks those exact campaign-grid bytes are outside the source tree, requires manifest SHA-256 equality to the generated grid, and requires repository `git status` unchanged before/after.
- **Repository changes:** `sipm_campaign_manifest.py`; `run_sensitivity_campaign.sh`; `submit_systematic.sh`; expanded `tests/test_sipm_campaign_manifest.py`; derivation archive `2026-08-12T052500Z_...md`; Git-object adversarial addendum `2026-08-12T054500Z_..._ADVERSARIAL.md`; grid/source correction archive `2026-08-12T060500Z_..._GRID-OUTPUT-CORRECTION.md`.
- **Validation boundary:** earlier focused/local fixtures are not authority for the new final head. Both push and pull-request MC Validation contexts must succeed on the exact post-correction head before integration. No production Geant4 campaign is claimed.
- **Four-role votes after second adversarial correction:** detector-response/provenance lead `ACCEPT source/output separation / REVISE pending CI / BLOCK #977 COMPLETE`; adversarial reviewer `REJECT relaxing clean-source gate / ACCEPT external content-bound grids`; independent validator `ACCEPT deterministic discriminator / REVISE pending protected CI / BLOCK detector inference`; claims/provenance reviewer `KEEP #977/#1067 OPEN/PARTIAL`.
- **Important residual:** this binds campaign source intent and generated grid bytes, not executable/toolchain bytes. A stale build can still differ from the manifest superproject commit. Direct/manual `sipm_sensitivity.py --expected-core-sha` remains operator-controlled unless separately manifest-bound. Shared-checkout launcher/verifier byte identity and historical sidecar authenticity remain open.
- **Children:** `ARU-SIPM-CAMPAIGN-POINT-MANIFEST-CONSUMER-001`; `ARU-SIPM-RUN-METADATA-BINARY-BUILD-MANIFEST-001`; `ARU-SIPM-CAMPAIGN-SUPERPROJECT-BUILD-CLOSURE-001`; `ARU-SIPM-CAMPAIGN-LAUNCHER-BYTE-BINDING-001`; `ARU-ELEC-IMPULSE-HISTORICAL-OUTPUT-AUDIT-001`; #1072; #1067.
- **Concurrent unrelated work:** draft PR #1279 remains DAQ S00 saturation authorization; do not absorb or overwrite it.
- **Next gate / next atom:** if exact-final-head protected CI validates #1284, integrate with expected-head guard. Then the next highest-value child is `ARU-SIPM-RUN-METADATA-BINARY-BUILD-MANIFEST-001` / superproject-build closure, because source intent still does not identify executable bytes.
- **Scientific boundary:** no beam bytes, production Geant4 detector population, measured electronics calibration, DATA↔MC result, timing/PID metric, pile-up efficiency, rate, ESS, p-value, or detector-performance quantity was generated or promoted.
