# Latest Handoff

## Source-bound SiPM campaign intent implemented; exact-head CI still required

Selected atom `ARU-SIPM-CAMPAIGN-MANIFEST-CORE-SHA-SOURCE-001` is `ACTIVE / PRE-CI` under open parent #977. The branch `audit/sipm-campaign-manifest-core-sha-source-v1` starts from exact protected `main@4abbf9112ad7933b4f0ec5b927f2d0f358ad08be`; that superproject commit pins `geant4/single_stave/sipm` to exact `ccb-sipm-core@3627dc87137a9f33f511a755671414b11853c0a0`.

The previous consumer gate from #1282 can enforce an optional exact expected core SHA, but an operator-provided token is not itself campaign provenance. The new `ccb-sipm-campaign-intent/1` helper removes that source ambiguity for the orchestrated campaign: it derives the expected core only from the exact superproject gitlink, records the superproject commit, base CLI, event count, single-thread intent, and selected grid SHA-256 values, emits canonical JSON, and refuses to overwrite an existing output directory with different campaign intent.

`run_sensitivity_campaign.sh` now freezes the manifest after grid generation, records its SHA-256, independently verifies it, and passes the literal digest into every Slurm job. `submit_systematic.sh` re-verifies the manifest and selected points-grid before launching the simulator, then requires the producer sidecar's compile-bound `digitizer.ccb_sipm_core_commit` to equal the manifest gitlink before writing `ccb-sipm-campaign-point/1`. The analyzer expectation in the orchestrated `--analyze` path is re-derived from the same frozen manifest rather than typed by an operator.

Executed software/provenance evidence: focused tests returned `7 passed in 0.04s`; Python compilation and both shell syntax checks succeeded; a temporary Git repository carrying the real core gitlink derived exact `3627dc...`; changing manifest content under a frozen digest failed with exit 3. A fake scheduler/executable integration fixture accepted the exact core path and wrote point campaign provenance, then rejected a changed grid with exit 3 before producing a new fake ROOT output. These are deterministic provenance fixtures, not detector/Geant4 validation. Ruff is unavailable in the local container, so protected repository CI remains mandatory.

Four sequential pre-CI review votes: detector-response/provenance lead `ACCEPT design / REVISE pending CI / BLOCK #977 COMPLETE`; adversarial provenance reviewer `ACCEPT mutation/substitution control / BLOCK executable-byte identity`; independent validation reviewer `ACCEPT deterministic oracle / BLOCK detector inference / REVISE pending CI`; claims/provenance reviewer `KEEP #977 and #1067 OPEN/PARTIAL`.

Material residuals: source intent does not prove which executable bytes/toolchain generated an output; direct/manual `sipm_sensitivity.py --expected-core-sha` remains an operator-controlled route unless separately bound to a manifest; pre-#1280 historical sidecars remain unauthenticated. Retain children `ARU-SIPM-CAMPAIGN-POINT-MANIFEST-CONSUMER-001`, `ARU-SIPM-RUN-METADATA-BINARY-BUILD-MANIFEST-001`, `ARU-SIPM-CAMPAIGN-SUPERPROJECT-BUILD-CLOSURE-001`, `ARU-ELEC-IMPULSE-HISTORICAL-OUTPUT-AUDIT-001`, #1072, and #1067.

Archive: `chatgpt_todo/archive/2026-08-12T052500Z_ARU-SIPM-CAMPAIGN-MANIFEST-CORE-SHA-SOURCE-001.md`. Unrelated draft PR #1279 remains DAQ saturation work and must not be absorbed. Next action is a bounded draft PR plus exact-final-head MC Validation; if integrated, the binary/build-manifest atom is the highest-value next provenance discriminator. No beam bytes, production Geant4 campaign, measured electronics calibration, timing/PID result, rate, ESS, p-value, or detector-performance claim was produced or promoted.
