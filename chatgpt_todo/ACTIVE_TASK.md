# Active Task

- **Selected atom:** `ARU-SIPM-CAMPAIGN-MANIFEST-CORE-SHA-SOURCE-001` is `ACTIVE / PRE-CI` on branch `audit/sipm-campaign-manifest-core-sha-source-v1`; parent #977 and cross-dependency #1067 remain `OPEN/PARTIAL`.
- **Exact base:** branch started from protected `main@4abbf9112ad7933b4f0ec5b927f2d0f358ad08be`; the exact SiPM gitlink at that commit is `ccb-sipm-core@3627dc87137a9f33f511a755671414b11853c0a0`.
- **Problem closed at design/software-fixture level:** #1282 can compare against `--expected-core-sha`, but a raw operator token is not content-bound campaign intent. The new `ccb-sipm-campaign-intent/1` creator has no expected-core input: it derives the core revision only from `git ls-tree HEAD geant4/single_stave/sipm`.
- **Implemented invariant:** for campaign intent `I`, `H_expected(I)=H_link(I)`; each orchestrated job requires `H_meta=H_expected`. Canonical manifest bytes and every selected points-grid are SHA-256 bound. The literal manifest digest is copied into Slurm argv, so post-submission manifest/grid mutation fails before simulation.
- **Repository changes:** new `scripts/single_stave/sipm_campaign_manifest.py`; new seven-test `tests/test_sipm_campaign_manifest.py`; updated `run_sensitivity_campaign.sh` to freeze intent and derive analyzer expectation from it; updated `submit_systematic.sh` to verify intent/grid before simulation and exact producer core afterwards; immutable derivation archive at `chatgpt_todo/archive/2026-08-12T052500Z_ARU-SIPM-CAMPAIGN-MANIFEST-CORE-SHA-SOURCE-001.md`.
- **Executed deterministic evidence:** focused unit suite `7 passed in 0.04s`; `python -m py_compile` succeeded; `bash -n` for both shell launchers succeeded. A temporary-gitlink fixture derived exact real core `3627dc...`; a manifest mutation returned exit 3. An end-to-end fake Slurm/executable fixture wrote a point campaign record only on exact core match; subsequent grid mutation returned exit 3 before fake simulator output. No RNG/Geant4/beam/detector population participated.
- **Local tooling boundary:** Ruff is not installed locally; no local Ruff PASS is claimed. Python sources were checked for >100-character lines after formatting. Protected exact-head MC Validation is required before integration.
- **Four-role pre-CI votes:** detector-response/provenance lead `ACCEPT design / REVISE pending CI / BLOCK #977 COMPLETE`; adversarial provenance reviewer `ACCEPT substitution/mutation control / BLOCK executable-byte identity`; independent validator `ACCEPT deterministic oracle / BLOCK detector inference / REVISE pending CI`; claims/provenance reviewer `KEEP #977/#1067 OPEN/PARTIAL`.
- **Important residual:** this binds campaign **source intent**, not executable bytes. A stale build can still differ from the manifest superproject commit. Direct/manual `sipm_sensitivity.py --expected-core-sha` remains an operator-controlled path unless it is separately required to consume the manifest.
- **Spawned children:** `ARU-SIPM-CAMPAIGN-POINT-MANIFEST-CONSUMER-001`; `ARU-SIPM-RUN-METADATA-BINARY-BUILD-MANIFEST-001`; `ARU-SIPM-CAMPAIGN-SUPERPROJECT-BUILD-CLOSURE-001`; `ARU-ELEC-IMPULSE-HISTORICAL-OUTPUT-AUDIT-001`; #1072; #1067.
- **Concurrent unrelated work:** draft PR #1279 remains DAQ S00 saturation authorization; do not absorb or overwrite it.
- **Next gate:** open a bounded draft PR, record stable concern on #977, and require exact-final-head protected CI. If this source-intent child integrates, next highest-value atom is the binary/build-manifest child.
- **Scientific boundary:** no beam bytes, production Geant4 detector population, measured electronics calibration, DATA↔MC result, timing/PID metric, pile-up efficiency, rate, ESS, p-value, or detector-performance quantity was generated or promoted.
