# Active Task

- **Selected atom:** `ARU-SIPM-RUN-METADATA-COMPILED-CORE-SHA-001` is `PARTIAL`, parent #977 with cross-dependency #1067.
- **Current protected base:** `ccb-testbeam/main@896c6c0bca2fa0d5fdf50a5d33840e4b8ab75b60`; exact gitlink `geant4/single_stave/sipm -> ccb-sipm-core@3627dc87137a9f33f511a755671414b11853c0a0`.
- **Confirmed defect:** `RunAction::WriteMetadataSidecar()` reads `digitizer.ccb_sipm_core_commit` from mutable `CCB_SIPM_CORE_COMMIT` and writes `unspecified` when absent, while the production sensitivity launcher exports only `CCB_GIT_COMMIT`. The same digitizer block advertises `validation_status=OK`; `sipm_sensitivity.py` accepts that plus a nonempty config digest without requiring a bound core SHA.
- **Atomic invariant:** `H_meta = H_compiled = H_link`. The numerical config/runtime-kernel digest is complementary to, not a substitute for, source revision identity.
- **Mechanisms:** caller-env identity and config-digest==code-identity are rejected; launch-time source-tree identity is insufficient against stale binaries; a compiled gitlink literal survives as the bounded repair. Full binary/toolchain provenance remains a child.
- **Implementation branch:** `audit/sipm-compiled-core-sha-binding-v1`. `SipmBuildProvenance.hh` freezes exact `3627dc...`; `SipmBuildProvenance.cc` is automatically linked by the existing `src/*.cc` CMake glob and overwrites the legacy environment bridge before `main()` with `setenv(..., overwrite=1)`, aborting on failure. Focused tests compare the literal to the superproject gitlink, compile/run a hostile-environment probe, and guard CMake composition.
- **Executed local fixture:** isolated C++17 compile under `-Wall -Wextra -Wpedantic`; running with `CCB_SIPM_CORE_COMMIT=deadbeef` printed exact `3627dc87137a9f33f511a755671414b11853c0a0`. This is software evidence only, not Geant4/detector validation.
- **Issue governance:** #977 and #1067 were reopened because their material acceptance leaves remain unresolved. Stable concern `CCB-977-COMPILED-CORE-SHA-001` is recorded on #977.
- **Four-role votes:** detector-response/provenance lead `ACCEPT bounded binding / BLOCK #977 COMPLETE`; adversarial reviewer `REJECT env/config equivalence / ACCEPT compiled literal`; independent validator `ACCEPT deterministic probe / BLOCK detector inference`; claims/provenance reviewer `REOPEN parents / BLOCK waveform and measured-electronics promotion`.
- **Archive:** `chatgpt_todo/archive/2026-08-12T035600Z_ARU-SIPM-RUN-METADATA-COMPILED-CORE-SHA-001.md`.
- **Children:** binary/build-manifest identity; historical sidecar audit; downstream sensitivity exact-core-SHA gate; #1072 requested/effective operating-point semantics; #1067 source-byte/calibration/positive-authorization leaves.
- **Next gate:** require protected exact-final-head MC Validation for the bounded PR. Do not merge on local fixture evidence alone. If green and current-main ancestry remains clean, integrate with expected-head guard; then audit historical sidecars and harden downstream sensitivity admission.
- **Scientific boundary:** no beam data, production Geant4 population, measured electronics calibration, timing/PID, pile-up efficiency, rate, ESS, p-value, or detector-performance result was generated or promoted.
