# Active Task

- **Task ID:** `ARU-MC-G4-LOADER-PROC-ENV-001`
- **Owner:** hourly Atomic Research Universe audit session
- **Protected main inspected:** `d6dc5ab29fc0ae6ac9d921a50c08b4554d14902d`, the squash merge of validated PR #1210. Exact predecessor head `ae6e8506f7caa79c31f211f56c7bb31761007600` passed MC Validation run `31476519812`; #1182 and CL-021 remain gated.
- **Branch:** `audit/geant4-loader-proc-env`, based exactly on that protected main.
- **Selected universe:** the scientific meaning and stability of the Linux `/proc/<pid>/environ` bytes already used by runtime provenance, as a child of `ARU-MC-G4-LOADER-SEARCH-001` under #1182.
- **Correction to prior handoff:** Linux procfs documents `/proc/<pid>/environ` as the initial environment region of the currently executing image, not a generic post-start `getenv` view. Ordinary later `setenv`/`putenv` changes are not reflected. However, the target can overwrite the initial bytes or relocate the referred region with `PR_SET_MM_ENV_START`, so a late snapshot is not an immutable historical `execve(envp)` trace.
- **Exact contract:** compose PASS runtime-dependency + PASS loader-secure-state receipts for the same `(pid,starttime_ticks)`; read `/proc/<pid>/environ` twice; require process identity stable, exact byte equality of the two reads, exact match to every loader-environment value already recorded by the runtime receipt, and fail closed on duplicate tracked keys. Serialize snapshot byte count/SHA-256 and one-sided presence semantics.
- **Interpretation:** presence in the stable procfs region is `OBSERVED_AT_ATTESTATION_BOUNDARY`; absence is `ABSENT_AT_OBSERVATION_NOT_PROOF_OF_EXECVE_ABSENCE`. `AT_SECURE=1` still makes `LD_LIBRARY_PATH`/`LD_PRELOAD`/`LD_AUDIT` non-authorising for search; `AT_SECURE=0` still does not prove effective non-secure glibc behavior.
- **Mechanisms eliminated:** procfs as current program-environment view; procfs as immutable execve log; launch environment as a complete loader-decision model.
- **Authoritative facts:** Linux `proc_pid_environ(5)`, Linux `ld.so(8)`, and GNU libc Dynamic Linking Tunables were used. A local glibc 2.41 secure-sanitization control showed loader variables absent from program `getenv` while exact launch strings remained in `/proc/<pid>/environ`; this is a mechanism falsifier, not HIBEAM validation.
- **Local execution:** Python 3.13.5/Linux/no RNG, `PYTHONPATH=/tmp/ccb_new python3 -m pytest -q /tmp/ccb_new/tests/test_geant4_loader_initial_environment_attestation.py` -> `10 passed in 0.54s`; `py_compile` passed. Local ruff is unavailable.
- **Exact content binding:** tool 13024 bytes, SHA-256 `a1d3074fcf998c17abf5d99752f399d98aca491f184cad704224ea08111ab9b3`, Git blob `ab0f087fd2a138101bd269b97afc8b607ccb9036`; tests 9730 bytes, SHA-256 `9a81c81ea51ac27e94d925635b9ba800d6acc1742f9409ca57e3c161f7e41203`, Git blob `f3c79dbf80a064a28885046dcbed08940f2f174f`.
- **Four-role state:** runtime/physics lead `REVISE prior post-start characterization / ACCEPT bounded initial-region presence`; adversarial Linux reviewer `ACCEPT stable observation / BLOCK immutable execve claim`; independent validation reviewer `ACCEPT deterministic mechanism oracle / BLOCK HIBEAM and physics generalisation`; claims/provenance reviewer `ACCEPT provenance refinement / BLOCK CL-021 promotion`.
- **Refined children:** `ARU-MC-G4-LOADER-PREEXEC-ENV-001` is not complete but is narrower: immutable launch absence or no-remap/no-overwrite evidence still needs a pre-exec boundary. New `ARU-MC-G4-LOADER-ENV-REGION-MUTATION-001` and `ARU-MC-G4-LOADER-ARGV-001`; existing cwd, cache/config, token/hwcaps, preload/audit, linker/static, late-dlopen, wrapper, immutable-consumption, runtime-manifest, source/stopping, weights and detector-response children remain open.
- **Repository files:** new attestor, hostile tests, curated ruff-list integration, and `chatgpt_todo/archive/2026-08-11T094700Z_ARU-MC-G4-LOADER-PROC-ENV-001.md` are on the branch.
- **CI/merge gate:** require fresh exact-final-head MC Validation with curated ruff + full non-integration pytest + diagnostics/enforcement and recheck current-main ancestry before any merge. No production HIBEAM runtime receipt exists in this session.
- **Status:** `ACTIVE / IMPLEMENTED_ON_BRANCH / LOCAL_FALSIFIERS_PASS / EXACT_HEAD_CI_REQUIRED / REAL_HIBEAM_RUNTIME_BLOCKED / PHYSICS_INFERENCE_BLOCKED`
