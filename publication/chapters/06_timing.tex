\section{Inter-stave timing}

\subsection{Observable and covariance}

For staves \(i\) and \(j\), define
\[
\Delta t_{ij}=t_j-t_i-t_{{\rm TOF},ij}.
\]
The pair width does not uniquely determine single-stave resolutions. In general,
\[
\mathrm{Var}(\Delta t_{ij})=\sigma_i^2+\sigma_j^2-2\,\mathrm{Cov}(t_i,t_j).
\]
Common trigger phase, sampling phase, reconstruction choices and event conditions may produce covariance. The paper therefore does not divide a pair width by \(\sqrt{2}\) without an independently justified model.

\subsection{Pulse-component definition}

Leading-edge timing is amplitude-dependent. Constant-fraction timing also becomes ambiguous for multi-component waveforms if the threshold is defined from the global maximum: changing the fraction can switch the timed physical component. The production method must define the selected pulse component before optimising any timing fraction. Same-sample minimum widths across a fraction scan are exploratory model-selection diagnostics, not performance measurements.

\subsection{Current 8-by-16 data diagnostic}

On the located 8-by-16 raw waveform product, the current direct B4--B6 CFD reconstruction returns 5,207 events with valid times and an approximately 38 ns central-68\% residual width (format-/reconstruction-limited; see the evidence box below) after the applied TOF subtraction. Peak positions are multi-modal and frequently lie near waveform boundaries. The component-safe rerun has been executed twice. The first (\#1335) used the locked v1 polarity convention and yielded 8.748~ns on 10{,}776 events; the polarity measurement study (\#954) subsequently falsified v1 for channels 2--7, invalidating that number. Under the measured polarity map v2 (\#1383), on the complete authorising Sample~II population (runs 58--63 and 65; 228{,}697 complete pairs above the 1000~ADC amplitude cut), the component-safe constant-fraction mode (20\% fraction, first-local-peak component selection per \#1059) with the 0.312~ns B4--B6 time-of-flight correction yields a pair-residual central-68\% width of 0.146~ns (1{,}000-replicate bootstrap interval 0.144--0.148~ns) with a median of $-9.67$~ns, superseding both earlier diagnostics. Publication promotion remains conditional: the synthetic two-pulse component-recovery and wrong-component validations now pass under v2, but the constant $-9.7$~ns inter-channel offset is an uncalibrated skew, real-pulse multi-component identity is not validated on beam data, and timing/TOF definitions remain analysis contracts rather than source-bound hardware quantities.

\begin{evidencebox}
The pair-residual width (0.146~ns under measured polarity v2, superseding the 8.748~ns v1-based rerun and the earlier 38~ns direct-mode diagnostic) is reported as a \textbf{pair residual at fixed amplitude selection}. The width lies far below the 10~ns sampling interval, which indicates strong sample-interpolation precision on high-SNR pulses above the 1000~ADC cut rather than a validated detector resolution; the constant $-9.7$~ns median offset, window phase, pulse-component ambiguity on real pulses, boundary truncation and timing-reference/TOF assumptions remain entangled. No intrinsic stave timing resolution is extracted, and the residual width is not divided by $\sqrt{2}$. The timing-residual calculation is governed by \nolinkurl{scripts/issue\_1320\_timing\_residual.py} with a MANIFEST entry tracking source provenance (\#1343); the authorising artifact is committed under \nolinkurl{reports/issue\_1320\_timing/} and bound as a gated figure below.
\end{evidencebox}

\GatedFigure{figures/final/timing_b4_b6_residual.pdf}{0.90}{B4--B6 pair timing residual from the source-bound 8-by-16 waveform product (Sample~II, component-safe CFD20, first-local-peak mode). NOT DETECTOR RESOLUTION.}{Component-safe rerun under measured polarity v2 (\#1383, \#1320): central-68\% width 0.146~ns (bootstrap 0.144--0.148~ns), 228{,}697 Sample~II pairs, TOF-corrected; synthetic validations pass, but the $-9.7$~ns channel-skew offset and real-pulse component identity remain unresolved, so the claim stays GATED and the figure is not rendered in the publication build. Artifact: \nolinkurl{reports/issue\_1320\_timing/}.}

Historical sub-nanosecond values are retained only as provenance history or simulation/method-closure diagnostics and must not appear in the abstract/conclusions as beam-data performance.
