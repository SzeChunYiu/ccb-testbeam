# Active Task

- **Selected atom:** `ARU-TIMING-CFD-COMPONENT-CROSSING-BINDING-001` is `PARTIAL`, child of #1059/#1063. Review of the merged Lane05 `first_local_peak` estimator found that its threshold amplitude could come from a selected local peak while the returned time still came from an earlier component that the selector had rejected.
- **Clean integration base:** protected `main@f350ad9ed824fff7c5cd9500d7c592f6a287fb21`. Draft PR #1259 (`audit/cfd-component-bound-crossing-v2`) is deliberately limited to the canonical CFD repair, focused tests, immutable archive, and coordination. Superseded #1250 was closed unmerged after a concurrent-main merge polluted its changed-file set; no force-push was used.
- **Atomic law:** for selected peak `(p,A_p)` and `T=f*A_p`, use the nearest pre-peak bracket `y[k] < T <= y[k+1]`, `k+1<=p`, and `t=k+(T-y[k])/(y[k+1]-y[k])`. If no pre-peak sample is below `T`, the selected rise is left-censored. `global_max` retains historical whole-waveform first-crossing semantics.
- **Primary falsifier:** `[0,40,0,50,100,50,0,0,500,1000,500]`; existing 5%-of-global selector rejects 40 ADC, selects 100 ADC at sample 4, and CFD20 has `T=20`. Pre-repair code returned `t=0.5` from the rejected bump; same-component crossing is `t=2.4` from samples 2→3.
- **Adversarial revision:** `[30,0,50,100,50,0,250,500,250]` falsified the first repair draft's reuse of global sample-0 censoring. Although sample 0 is above the selected threshold, the trace returns below threshold before the selected rise, which is observed at `t=1.4`.
- **Exact-blob execution:** source blob `4aa845e2cb41c96cf70f010f135758e8fb94f5ae` and focused-test blob `00a686df5a83690caabb51751bd8ace9d72d0c50` were reconstructed with matching `git hash-object` identities. Python 3.13.5 / NumPy 2.3.5 / pytest 9.0.2: `python -m pytest -q /tmp/ccb_exact/tests/test_cfd_component_binding.py` -> `5 passed in 0.07s`. No RNG. This is software semantics only, not detector validation.
- **Protected CI gate:** both pull-request and push MC Validation contexts must pass on the exact final #1259 head before readiness/merge. Do not substitute earlier green heads or the focused local oracle.
- **Issue governance:** stable concern `CCB-1059-COMPONENT-CROSSING-BINDING-001` was added to existing #1059. #1059 remains OPEN because real-data component-transition decomposition, peak-selection validity, overlap ambiguity, held-out truth transfer, and downstream regeneration remain unresolved.
- **Four-role votes:** timing/estimator lead `ACCEPT same-component contract / BLOCK detector-truth inference`; adversarial waveform reviewer `REVISE first draft`, then `ACCEPT corrected semantics conditional on CI`; independent validation reviewer `ACCEPT exact-blob deterministic oracle / BLOCK timing-resolution inference`; claims/provenance reviewer `ACCEPT bounded repair / BLOCK #1059 completion and public timing-claim promotion`.
- **Highest-value next child:** `ARU-TIMING-CFD-PEAK-SELECTION-IDENTIFIABILITY-001`. The current `min_prominence_frac` implementation is actually a `0.05*global_max` amplitude floor plus local-maximum condition, not topographic prominence; its noise/overlap/saturation/domain-shift behavior is unvalidated.
- **Other children:** `ARU-TIMING-CFD-OVERLAP-BASIN-001`, `ARU-TIMING-CFD-REALDATA-TRANSITION-001`, `ARU-TIMING-CFD-TRUTH-TRANSFER-001`, and regeneration/audit of any timing study consuming `first_local_peak`.
- **Archive:** `chatgpt_todo/archive/2026-08-11T215500Z_ARU-TIMING-CFD-COMPONENT-CROSSING-BINDING-001.md`.
- **Scientific boundary:** no immutable beam waveform population, production Geant4/MC, timing resolution, pile-up mechanism/rate, WLS response, PID metric, ESS, p-value, or detector-performance result was regenerated or promoted.
- **Status:** `COMPONENT_BOUND_REPAIR_IMPLEMENTED / EXACT_BLOB_FOCUSED_TEST_PASS / FINAL_HEAD_CI_PENDING / #1059_OPEN / TIMING_CLAIMS_GATED`.
