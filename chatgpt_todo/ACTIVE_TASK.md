# Active Task

- **Selected atom:** `ARU-SIPM-BUILD-SUBMODULE-WORKTREE-IDENTITY-001` under open #977; status `ACTIVE / CI-REPAIR` in draft PR #1294.
- **Current integration lineage:** branch was advanced without force-push through merge commit `363cf3fd770c48aca578cf48e37fd7de1234ab72`, combining prior atom head `9232bed4b14531eb2bafa16ebcedb529b5a0347b` with protected `main@f0b41ba5a1d38bc01a0eb96be015f875b8363772` while rebuilding the tree from current main plus only the bounded atom files.
- **Contract:** an authorising build receipt requires exact superproject mode-160000 gitlink, a materialized independent nested core Git top-level at `geant4/single_stave/sipm`, nested `HEAD == gitlink`, and nested tracked/untracked cleanliness in addition to outer-tree, executable, cache/toolchain and Geant4-sentinel checks. Receipt schema is `ccb-single-stave-build-receipt/2`; v1 is non-authorising.
- **Executed exact-head falsifier:** push MC Validation `31575340395`, job `94046076935`, on superseded head `ae594fdd27a8863ded632ffc21451a200c6d854c` passed exact SiPM-core checkout/build/7 CTests, ruff and governance, but full pytest returned `1 failed, 2152 passed, 2 skipped, 8 xfailed, 1 xpassed`; enforcement correctly failed with `PYTEST_STATUS=1`.
- **Failure localization:** the sole failing campaign-launcher fixture created a detached superproject worktree but did not materialize the nested SiPM repository before invoking receipt v2. That state is intentionally non-authorising; weakening production `_core_worktree_identity()` is rejected.
- **Fixture repair:** commit `9232bed4b14531eb2bafa16ebcedb529b5a0347b` materializes an offline exact nested repository from the already checked-out CI core, detaches it at the gitlink SHA, verifies nested top-level/HEAD, and then exercises the launcher. No network is required inside the unit fixture.
- **Review votes:** build/provenance lead `ACCEPT v2 contract / REVISE fixture`; adversarial reviewer `REJECT gitlink-only fallback`; independent validator `ACCEPT failed CI as falsifier / REVISE pending final-head CI`; claims/provenance reviewer `KEEP #977 OPEN/PARTIAL`.
- **Residual children:** `ARU-SIPM-BUILD-IGNORED-SOURCE-INFLUENCE-001`; mutate-and-restore/TOCTOU; exact compiler/link invocation; runtime loaded-library identity; shared launcher/verifier byte binding; historical-output authenticity; #1072; #1067.
- **Next gate:** refresh `HANDOFF.md` and add immutable CI-correction archive, then require protected MC Validation on the resulting exact final PR head. Merge only with expected-head guard after all required contexts pass. No detector/beam result is promoted by this atom.
