# Raw PID notebook execution trigger

This marker triggers the dedicated pull-request workflow after the full analysis and notebook sources were assembled. It contains no scientific result and may be removed after the executed artifact is verified.
