# Active Task

- **Selected atom:** `ARU-TIMING-CFD-PEAK-SELECTION-IDENTIFIABILITY-001` is `PARTIAL`, child of #1059/#1063. The first-local-peak selector is now an explicit, non-authorising hypothesis rather than an implicit detector-truth component label.
- **Protected main inspected:** branch created from exact protected `main@75b80839042a367e54743401cc2d11cfab6d4c3b`; required status context is `test`.
- **Parent dependency:** current protected main still lacked the component-bound crossing repair from draft #1259 at branch creation. #1274 therefore carries that bounded dependency plus new selector diagnostics; #1259 must remain unmerged until #1274 is validated/supersedes it cleanly.
- **Atomic selector law:** with `A_g=max(y)` and default `alpha=0.05`, interior `j` is eligible when `y[j]>=y[j-1]`, `y[j]>=y[j+1]`, and `y[j]>=alpha*A_g`; select the earliest eligible `j`, else fall back to `argmax(y)`. Draft #1274 now rejects nonfinite or out-of-domain `alpha` outside `[0,1]`. This is a global-height floor, not topographic prominence.
- **Identifiability margin:** `m_j=A_j-alpha*A_g`. With fixed global-max identity and bounded amplitude perturbations, `|m_j| > eps_j + alpha*eps_g` is sufficient to prevent the floor decision flipping. Neighbour ordering and global-max identity require separate margins; plateaus have zero uniqueness margin under `>=`.
- **Corrected prominence falsifier:** `[0,50,51,50,50,500,1000,500]` has global floor 50 ADC and selects the 51-ADC early peak, while its left/right basin minima are 0 and 50, so topographic prominence is `51-max(0,50)=1` ADC. The first draft used a zero right basin and incorrectly called its prominence 1 ADC; adversarial review caught that error before merge and the fixture was corrected.
- **Other deterministic falsifiers:** changing an early peak from 49.9 to 50.1 ADC moves selected index from the late 1000-ADC peak to the early peak; a 100/100/100 plateau creates three eligible selector samples; monotonic boundary maxima fall back to the global selector.
- **Implementation:** draft PR #1274, branch `audit/cfd-selector-identifiability-v1`, adds named profile `first_local_peak_global_fraction_floor_v1`, state `HYPOTHESIS_UNVALIDATED_COMPONENT_IDENTITY`, `authorising_component_identity=false`, detailed selector diagnostics, component-bound crossing semantics from #1259, and ten deterministic tests.
- **Implementation provenance:** code commit `5cf030d2e50e326903f76b077a7234fbc31ba8a0`; parameter-domain fix `a0d8910986292fdfbf614fb830ea75e13b5a38ae`; corrected prominence-test commit/current pre-coordination code-test head `ee070d6bce20b327a5b830b6d01166e0266e8ace`; archive commit began at `6dc182bb280979b9ca62637f0f3c235caeeef2e4` and is corrected before merge.
- **Execution boundary:** a local sparse clone failed before checkout because the execution container could not resolve `github.com`, so there is no local pytest PASS claim. Every branch-doc or test correction changes the head and requires fresh exact-head push + pull-request `test` contexts before readiness/merge.
- **Stable concern:** `CCB-1059-PEAK-SELECTOR-IDENTIFIABILITY-001` was updated with the corrected prominence derivation and explicit adversarial `REVISE` history. #1059 remains OPEN/PARTIAL.
- **Cross-atom governance:** #968 was found closed as completed even though merged PR #1239 explicitly lists B2 mechanism discrimination as `BLOCKED / out of scope ... need dedicated studies/data`. #968 was reopened as physics-BLOCKED; this does not revert #1239 software fixes.
- **Four-role votes:** timing lead `ACCEPT explicit profile/diagnostics / BLOCK detector-truth validation`; adversarial reviewer `REVISE first prominence fixture`, then `ACCEPT corrected terminology falsifier / BLOCK authorising component identity`; independent validation reviewer `ACCEPT deterministic diagnosis / BLOCK timing-resolution inference`; claims/provenance reviewer `ACCEPT bounded fail-closed state / KEEP #1059 and #968 OPEN`.
- **Archive:** `chatgpt_todo/archive/2026-08-12T003900Z_ARU-TIMING-CFD-PEAK-SELECTION-IDENTIFIABILITY-001.md`.
- **Next highest-value child:** `ARU-TIMING-CFD-REALDATA-TRANSITION-001` if immutable beam bytes are available; otherwise `ARU-TIMING-CFD-NOISE-PHASE-SATURATION-SENSITIVITY-001` is the strongest valid fixture-level discriminator.
- **Other children:** `ARU-TIMING-CFD-OVERLAP-BASIN-001`, `ARU-TIMING-CFD-TRUTH-TRANSFER-001`, plus report/figure/claim regeneration for consumers of `first_local_peak`.
- **Claim boundary:** the legacy real-data CFD report remains FLAWED/QUARANTINED. No beam timing resolution, pile-up mechanism/rate, WLS response, PID metric, efficiency, rate, ESS, p-value or detector-performance claim is promoted.
- **Status:** `SELECTOR_PROFILE_EXPLICIT / CORRECTED_IDENTIFIABILITY_COUNTEREXAMPLES / FINAL_HEAD_CI_REQUIRED / #1059_OPEN / #968_REOPENED_BLOCKED / TIMING_CLAIMS_GATED`.
