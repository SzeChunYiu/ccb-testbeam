# Active Task

- **Task ID:** `ARU-SIPM-PREWINDOW-KERNEL-COVERAGE-001`; bounded numerical/electronics-support child validated in core, parent #1096 remains open/PARTIAL.
- **Owner:** hourly Atomic Research Universe audit session.
- **Protected testbeam main at selection:** `a83d3b64b87d3781211a2db42d5375bea8d4ae41`, integrating `ccb-sipm-core@f009b0d6d0ccd4f54ef9be62b793934a56e518` after the dark-history repair.
- **Exact contract:** for every admitted avalanche `t_a` and recorded sample `t_i`, `Waveform.signal_pe[i]` must include `A*h(t_i-t_a)` whenever `t_i>=t_a` and the declared causal impulse is nonzero. With `N_out=floor((window_end-window_start)/dt)+1` and `H=max(0,window_start-history_start)`, a conservative uniform-grid kernel is `N_kernel=N_out+ceil(H/dt)`.
- **Confirmed pre-repair contradiction:** core main admitted avalanche and DCR history from `history_start_ns`, but `make_waveform()` allocated and iterated an impulse kernel only `n_samples` long, where `n_samples` is the recorded waveform count. At defaults (`history=-200 ns`, window `[-20,250] ns`, `dt=0.5 ns`) `N_out=541`, full grid coverage requires 901 kernel samples, and the earliest avalanche is 450 ns old at the final sample while the old kernel ends at 270 ns elapsed time.
- **Deterministic discriminator:** fixture `history=-20 ns`, window `[0,10] ns`, `dt=1 ns`, one avalanche at the history boundary, generic CR-RC `rise=1 ns`, `decay=25 ns`, PDE=1 with dark/crosstalk/afterpulse/gain/SPTR/electronics-noise fluctuations disabled. Old code yields an all-zero waveform because its 11-point kernel covers ages 0--10 ns while output needs ages 20--30 ns. Corrected 31-point analytic reference is nonzero at every sample (~0.536747 first, ~0.359792 last).
- **Negative controls:** `history_start=window_start` preserves exact legacy grid semantics; a measured finite-support impulse `[0,10] ns` with amplitudes `{0,1,0}` remains exactly zero for an avalanche aged 20--30 ns, so numerical extension does not invent physical support.
- **Core implementation:** PR #5 on branch `audit/prewindow-kernel-coverage` changed `make_waveform()` to add `ceil((window_start-history_start)/dt)` elapsed-time kernel samples and convolve over `h.size()`, added `tests/test_kernel_history.cc`, and wired it into CTest.
- **Core execution:** exact PR head `454579870a8f1eb9b0f67e02df30486f778f7b67` passed Core CI run `31520345468`, job `93875571412`: checkout/configure/build/test all SUCCESS. PR #5 squash-merged with expected-head guard as `ccb-sipm-core@68eadf3f6ac3a95b37e0d8c86843573736a5ea97`; exact merged-main push Core CI run `31520555652`, job `93876260437`, independently passed checkout/configure/build/test.
- **Testbeam integration:** branch `fix/sipm-kernel-history-integration`, based on exact `main@a83d3b64...`; gitlink commit `76e6c2bf02d4000ffeaa9b2c79b0dd362acd66ef` advances only `geant4/single_stave/sipm` to core `68eadf3f...`. Draft PR #1227 owns exact testbeam protected-CI integration. Execution archive: `chatgpt_todo/archive/2026-08-11T180300Z_ARU-SIPM-PREWINDOW-KERNEL-COVERAGE-001_EXECUTION.md`.
- **Four-role state:** SiPM/electronics lead `ACCEPT bounded numerical support / REVISE physical response+horizon`; adversarial reviewer `ACCEPT elapsed-time coverage / BLOCK sampling-aperture inference`; independent validation reviewer `ACCEPT deterministic software oracle / BLOCK detector inference`; claims/provenance reviewer `ACCEPT child VALIDATED after integration / BLOCK #1096 COMPLETE and claim promotion`.
- **Coordination conflict:** testbeam PR #1226 currently points the gitlink to core `5c6bb027...`, which is based on `2027b06...` and therefore does not contain validated dark-history core main `f009b0d...` or the new kernel-history core `68eadf3f...`. A blocking comment was added; measured-impulse work needs rebasing/cherry-picking onto current core main and fresh exact-head validation. An attempted automatic conversion of #1226 back to draft was blocked by the connector safety classifier and is not claimed as completed.
- **Cross-scale boundary:** this validates only numerical convolution support. The physical history horizon, real CCB electronics impulse, off-grid arrival/sampling aperture, downstream waveform reconstruction, timing, baseline, pile-up, PID, and DATA↔MC compatibility remain unresolved. No measured or production-simulation detector result is promoted.
- **Surviving children:** `ARU-SIPM-HISTORY-HORIZON-CONVERGENCE-001`, `ARU-SIPM-SAMPLING-APERTURE-001`, measured/generic electronics calibration, and downstream waveform→timing/baseline/pile-up propagation.
- **Next highest-value atom:** `ARU-SIPM-HISTORY-HORIZON-CONVERGENCE-001`, after exact-head testbeam integration CI/merge. The required question is how far prehistory must extend for recovery, afterpulse/crosstalk ancestry and electronics tails to make in-window observables stable under further history extension.
- **Status:** `ACTIVE / KERNEL_COVERAGE_CHILD_VALIDATED_IN_CORE / CORE_PR5_MERGED_AND_EXACT_MAIN_CI_GREEN / TESTBEAM_PR1227_DRAFT / #1096_PARTIAL / PHYSICAL_HISTORY_HORIZON_OPEN / DETECTOR_CLAIMS_GATED`.
