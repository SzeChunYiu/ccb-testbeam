# Active Task

- **Task ID:** `ARU-MC-G4-LINK-METADATA-001`
- **Owner:** hourly Atomic Research Universe audit session
- **Protected main inspected:** `2db54689253ac993d2cf430ebc7ee7e8173ec7c7`.
- **Validated predecessor:** PR #1204 / `ARU-MC-G4-RUNTIME-MAPS-001` is on protected main as `1b6608b8e106fe8ec1f73f6e40918a8c75d091f9`; #1205 handed off this link-metadata child. #1204 exact-head MC Validation run `31450233217` succeeded with curated ruff clean and `1511 passed, 1 skipped, 8 xfailed, 1 xpassed`.
- **Selected universe:** exact executable ELF link metadata (`PT_INTERP`, `DT_NEEDED`, `DT_SONAME`, `DT_RPATH`, `DT_RUNPATH`) composed with the content-bound live mapping receipt. Keep configured package identity, static link declarations, and observed mappings as distinct observables.
- **Derived mechanism:** parse the already-hashed ELF64/x86-64 bytes directly rather than trusting `ldd` or an unattested `readelf`; collapse duplicate identical dependency declarations for closure counting while retaining raw order; match non-path `DT_NEEDED` to exactly one content-rebound runtime object by `DT_SONAME`; match absolute-path dependencies and `PT_INTERP` by content identity; block relative path dependencies until runtime cwd provenance exists; record mapped-but-not-direct objects rather than assuming they are impossible.
- **Executed local falsifier:** Python 3.13.5, Linux, no RNG. `PYTHONPATH=. python -m pytest -q tests/test_geant4_elf_link_attestation.py` -> `11 passed in 0.08s`; `python -m py_compile tools/audit/geant4_elf_link_attestation.py` passed. Direct parser smoke on local `/bin/ls` and `/usr/bin/python3` also returned their interpreter/direct-dependency metadata. These are software tests, not Geant4 validation.
- **Repository state:** branch `audit/geant4-elf-link-metadata` was fast-forwarded to exact current main. The immutable ARU record is committed there as `d49e34f0dbfb9e5f24e035b2a5740ed9950327a6`.
- **Write blocker:** the complete bounded implementation/test files were attempted through authenticated GitHub `create_file` and `create_blob`, but both code writes were rejected by the tool safety classifier as indeterminate. No code commit, PR CI result, or merge is claimed. Documentation/coordination writes remain available.
- **Parent dependency:** #1182 / compiled Geant4 provenance. #1178, #1179, #1058, #1053/#880 and CL-021 remain gated.
- **Child universes spawned:** `ARU-MC-G4-RUNTIME-LINK-COOBSERVATION-001`, `ARU-MC-G4-LINK-COMMAND-001`, `ARU-MC-G4-LOADER-SEARCH-001`; existing late-`dlopen`, mapped-page, wrapper-chain, immutable-consumption, and runtime-manifest children remain.
- **Separate repository-provenance observation:** current main's newest commit introduces many tracked Python 3.13 `__pycache__/*.pyc` files. That is a distinct cleanup leaf and not evidence about link or Geant4 physics.
- **Status:** `PARTIAL / DETERMINISTIC_LOCAL_FALSIFIERS_PASS_11 / ARU_RECORD_ON_BRANCH / CODE_WRITE_SAFETY_BLOCKED / REAL_HIBEAM_RUNTIME_BLOCKED / DETECTOR_INFERENCE_BLOCKED`
