# Latest Handoff

## SiPM sensitivity core-revision admission gate is integrated; historical authenticity remains open

`ARU-SIPM-SENSITIVITY-CORE-SHA-GATE-001` is now `VALIDATED` at bounded downstream software/provenance scope. PR #1282 final head `7b09b68a36145a501531d2a061c6b297714d6d7d` passed both exact-head protected MC Validation contexts — push `31564970064` and pull request `31564993781` — before it was marked ready and squash-merged with an expected-head guard. Protected remote main became exact `9d74a2a2c94227a4ea206c2921a9a8f137b176a9`; the SiPM gitlink remains `ccb-sipm-core@3627dc87137a9f33f511a755671414b11853c0a0`.

The integrated consumer now rejects missing/noncanonical `digitizer.ccb_sipm_core_commit`, rejects mixed exact core revisions within one knob and across one campaign summary, and optionally requires exact equality to `--expected-core-sha`. The numerical `digitizer_config_sha256` remains separately required because configuration identity and implementation identity are different provenance variables. Focused hostile controls cover missing SHA, short `deadbeef`, valid-but-unexpected full SHA, and mixed full SHAs; exact match remains the positive control.

The important boundary is unchanged by the green CI: a syntactically canonical, campaign-consistent historical SHA is not necessarily authentic. Before the producer-side #1280 repair, caller/environment state could populate the field. Therefore neither this gate nor PR #1282 retroactively validates old sensitivity outputs. `ARU-ELEC-IMPULSE-HISTORICAL-OUTPUT-AUDIT-001` remains required for historical artifacts, and `ARU-SIPM-CAMPAIGN-MANIFEST-CORE-SHA-SOURCE-001` must define a content-bound source for an external expected SHA rather than relying on operator memory or an untracked CLI token.

Four sequential AI reviews after protected CI: **detector-response/provenance lead** `ACCEPT bounded child VALIDATED / BLOCK #977 COMPLETE`; **adversarial provenance reviewer** `ACCEPT canonical+homogeneity enforcement / REVISE historical-authenticity inference`; **independent validation reviewer** `ACCEPT exact-head CI / BLOCK detector inference`; **claims/provenance reviewer** `KEEP #977 and #1067 OPEN/PARTIAL`. Stable concern `CCB-977-SENSITIVITY-CORE-SHA-GATE-001` and final merge evidence are recorded on #977.

Derivation archive: `chatgpt_todo/archive/2026-08-12T050300Z_ARU-SIPM-SENSITIVITY-CORE-SHA-GATE-001.md`. A separate integration addendum records final CI/merge state without rewriting that pre-merge derivation. Post-merge main MC Validation run `31565320321` started on exact `main@9d74a2a...`; at follow-up creation it was queued, so do not claim its PASS until verified.

Next highest-value atom: `ARU-SIPM-CAMPAIGN-MANIFEST-CORE-SHA-SOURCE-001`. Its contract should bind `H_expected` to immutable campaign intent, specify source bytes/schema/hash and relation to the superproject/core revisions, reject mutable/operator-only expectations, and provide hostile substitution/stale-manifest controls. If immutable historical sidecars are available, the historical-output audit is an equally legitimate parallel high-value lane.

Other material children remain `ARU-SIPM-RUN-METADATA-BINARY-BUILD-MANIFEST-001`, #1072 requested/effective operating-point semantics, and #1067 measured-impulse source/calibration authorization. Open PR #1279 is unrelated DAQ S00 saturation work and must not be absorbed. No beam bytes, production Geant4 detector population, measured electronics calibration, timing/PID result, rate, ESS, p-value, or detector-performance claim was produced or promoted.
