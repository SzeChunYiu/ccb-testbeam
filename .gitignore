# ---- Large data: never in git. Canonical store is /home/billy/ccb-data (immutable,
# outside the repo). `data` is a read-only symlink into it. See DATA.md. ----
data/
/data
*.zip
*.tar
*.gz
*.root
*.h5
*.hdf5
*.npy
*.npz
*.parquet
*.feather

# ---- Source PDFs (provenance only; tracked separately if needed) ----
*.pdf

# ---- ML artifacts ----
*.pt
*.pth
*.ckpt
*.pkl
*.joblib
checkpoints/
runs/
wandb/
mlruns/

# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv/
.env
.ipynb_checkpoints/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---- Editors / OS ----
.vscode/
.idea/
.DS_Store
*.swp

# ---- Scratch ----
scratch/
tmp/
*.log

# SLURM job logs
geant4/jobs/*.out
geant4/jobs/*.err

# Generated CMake/Geant4 build trees are local, environment-specific artifacts.
geant4/**/build/

# MC validation ephemeral artifacts
reports/mc_validation_fixture/
reports/mc_validation/mv0_digitizer/
reports/mc_validation/mv9_synthesis/
reports/mc_validation/runs/
reports/mc_validation/resolved_configs/
reports/mv0_digitizer_skeleton/
RUN_BLOCKED.md
src/ccb_mc_validation.egg-info/
*.parquet.tmp

# ---- Generated study reports & publication builds (REP-002) ---------------
# Canonical outputs are rebuildable from scripts/ and must not be committed.
# Existing tracked artifacts are NOT mass-deleted (out of scope); these rules
# only prevent NEW generated outputs from being added. See REPORTS_HYGIENE.md.
/reports/data01_sample_split*/
/reports/mv3_stopping_v2*/
/reports/mv3_stopping_v3*/
/paper/figures/

# ---- Opticks GPU integration build + scratch (never in git) ----
build/
build-gpu/
build-*/
# keep figures/opticks/SUMMARY.md + the committed diagnostic png is re-added explicitly below
parity/
opticks_probe/
optical_gpu*/
*.npy
*_probe/
ccb_dump_gdml
export_check.gdml
exported.gdml
