# Active Task

- **Task ID:** AUD-LEDGER-001
- **Owner:** scheduled scientific-review session
- **Session stamp:** 2026-07-24T072257Z
- **Initial remote main SHA for this unit:** `712adba593c9b84e4617c1fe8013873cd0c5f753`
- **Scope completed in this unit:** audited the MV0 v2 gain chain behind malformed ledger rows `CL-013` and `CL-014`; compared the claim ledger, report, calibration artifact, tracked producer at its recorded source commit, and figure registry; confirmed an unreproducible methodology/code/output-schema/command conflict; added a fail-closed provenance audit, focused regression, JSON evidence, and accessible SVG evidence.
- **Implemented files:** `tools/audit/audit_mv0_gain_provenance.py` v1.0.0; `tests/test_audit_mv0_gain_provenance.py`; `docs/validation/mv0_gain_provenance_{audit.md,validation.json,svg}`.
- **Validation:** `py_compile` passed; focused pytest returned `4 passed in 0.64s`; the current-like fixture returned status 1 with ten findings; the corrected fixture returned `VALIDATED`; JSON and SVG parsed; changed Python lines were at most 100 characters. Repository facts and exact Git blob identities were checked through authenticated GitHub reads because a complete checkout was unavailable.
- **Measured current state:** `CL-013` has 38 fields and `CL-014` has 37 under the canonical 43-column header. The committed calibration artifact declares net amplitude `abs(amplitude_adc - baseline_adc)`, while the tracked producer uses raw `amplitude_adc` for both global and per-stave fits. The report command uses `--data`, omits required `--truth-npz`, and cannot reproduce the artifact from the declared producer. Ledger and figure provenance cite stale/nonexistent producer/result paths.
- **Independent calculation:** `1781 / (26.44 * 0.733) = 91.89639906462777 ADC/MeV`, consistent with the artifact's central value of 92. The artifact supplies a 30% systematic but no statistical uncertainty or formal confidence interval; ledger values `stat_unc=14`, `total_unc=31.3`, and `[60,124]` are unsupported by this artifact.
- **Scientific boundary:** no raw pulse table, ROOT file, NPZ truth file, calibration rerun, KS recomputation, bootstrap, detector calibration, or downstream performance result was produced. The audit/evidence is validated; the gain claim is not.
- **Remaining work:** withhold `CL-013`/`CL-014` from canonical validated use; recover or repair a producer that implements the documented net-amplitude method; fix the reproduction command and output schema; rerun on immutable inputs with hashes; establish statistical/systematic uncertainty and interval construction; reconstruct both rows to exactly 43 fields; repair `FIG-EN-001`; then continue source-backed reconstruction of the remaining malformed ledger rows.
- **Status:** PARTIAL.
