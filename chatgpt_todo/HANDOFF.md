# Latest Handoff

## Source-bound SiPM campaign intent corrected under adversarial review; exact-head CI required

Selected atom `ARU-SIPM-CAMPAIGN-MANIFEST-CORE-SHA-SOURCE-001` is `ACTIVE / PRE-CI` in draft PR #1284 under open parent #977. The branch started from protected `main@4abbf9112ad7933b4f0ec5b927f2d0f358ad08be`; that commit pins `geant4/single_stave/sipm` to exact `ccb-sipm-core@3627dc87137a9f33f511a755671414b11853c0a0`. Base main-push MC Validation run `31565791869` is confirmed `SUCCESS`.

The previous #1282 consumer can enforce an optional expected core SHA, but an operator-provided token is not campaign provenance. The new `ccb-sipm-campaign-intent/1` lane derives expected core from the superproject gitlink, freezes canonical manifest bytes and selected grid SHA-256 values, copies the literal manifest digest into Slurm argv, verifies intent/grid before simulation, and requires the produced compile-bound core SHA to equal campaign intent before writing `ccb-sipm-campaign-point/1`.

The adversarial pass found and repaired a material first-draft weakness: a manifest with the right `SUPERPROJECT_GITLINK:...` source-label string could still claim an arbitrary core SHA. The corrected verifier now takes a repository root, resolves the manifest's exact recorded `superproject_commit`, runs the semantic equivalent of `git ls-tree <commit> geant4/single_stave/sipm`, requires a gitlink entry, and requires that exact object SHA to equal `expected_core.commit`. A new hostile test creates a temporary Git repository whose gitlink is `333...`, constructs an otherwise canonical manifest claiming `fff...`, and requires rejection. The corrected focused suite returns `8 passed in 0.06s`; Python compilation and both shell syntax checks remain clean. The original derivation is preserved at `2026-08-12T052500Z_...md`; the correction is preserved in `2026-08-12T054500Z_..._ADVERSARIAL.md` rather than rewriting history.

Four sequential pre-CI reviews after correction: detector-response/provenance lead `ACCEPT corrected source binding / REVISE pending CI / BLOCK #977 COMPLETE`; adversarial provenance reviewer `REVISE first draft, ACCEPT Git-object semantic check / BLOCK executable-byte identity`; independent validation reviewer `ACCEPT eight-test deterministic oracle / BLOCK detector inference / REVISE pending CI`; claims/provenance reviewer `KEEP #977 and #1067 OPEN/PARTIAL`.

Material residuals remain. Source intent does not prove which `ccb_stave_sim` binary/toolchain generated an output. Direct/manual `sipm_sensitivity.py --expected-core-sha` remains operator-controlled unless separately manifest-bound. The job still invokes verifier/helper bytes from a shared checkout; full launcher/verifier/build-byte identity belongs to the binary/build-manifest family. Historical pre-#1280 sidecars remain unauthenticated. Retain `ARU-SIPM-CAMPAIGN-POINT-MANIFEST-CONSUMER-001`, `ARU-SIPM-RUN-METADATA-BINARY-BUILD-MANIFEST-001`, `ARU-SIPM-CAMPAIGN-SUPERPROJECT-BUILD-CLOSURE-001`, `ARU-SIPM-CAMPAIGN-LAUNCHER-BYTE-BINDING-001`, `ARU-ELEC-IMPULSE-HISTORICAL-OUTPUT-AUDIT-001`, #1072 and #1067.

Draft PR #1284 is the only intended integration vehicle for this atom; unrelated draft PR #1279 remains DAQ saturation work and must not be absorbed. Require exact-final-head push and pull-request MC Validation before merge. No beam bytes, production Geant4 campaign, measured electronics calibration, timing/PID result, rate, ESS, p-value, or detector-performance claim was produced or promoted.
