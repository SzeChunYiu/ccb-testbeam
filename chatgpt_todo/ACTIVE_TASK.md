# Active Task

- **Selected atom:** `ARU-SIPM-CAMPAIGN-MANIFEST-CORE-SHA-SOURCE-001` is `ACTIVE / PRE-CI` in draft PR #1284; parent #977 and cross-dependency #1067 remain `OPEN/PARTIAL`.
- **Exact base:** branch started from protected `main@4abbf9112ad7933b4f0ec5b927f2d0f358ad08be`; the exact SiPM gitlink at that commit is `ccb-sipm-core@3627dc87137a9f33f511a755671414b11853c0a0`. Current-main push MC Validation run `31565791869` completed `SUCCESS` on those base bytes.
- **Implemented invariant:** for campaign intent `I`, `H_expected(I)=H_link(I)`; each orchestrated job requires `H_meta=H_expected`. Canonical manifest bytes and every selected points-grid are SHA-256 bound. The literal manifest digest is copied into Slurm argv, so post-submission manifest/grid mutation fails before simulation.
- **Adversarial correction:** the first implementation structurally trusted the manifest's `SUPERPROJECT_GITLINK:...` source label. That was insufficient: a canonical forged manifest could pair the label with an arbitrary 40-hex core. The corrected verifier now resolves the manifest's exact `superproject_commit` with `git ls-tree <commit> geant4/single_stave/sipm` and requires the returned gitlink SHA to equal `expected_core.commit`. Stable correction is archived separately.
- **Repository changes:** new `scripts/single_stave/sipm_campaign_manifest.py`; eight-test `tests/test_sipm_campaign_manifest.py`; updated `run_sensitivity_campaign.sh`; updated `submit_systematic.sh`; derivation archive `2026-08-12T052500Z_...md`; adversarial addendum `2026-08-12T054500Z_..._ADVERSARIAL.md`.
- **Executed deterministic evidence:** corrected focused suite `8 passed in 0.06s`; `python -m py_compile` succeeded; `bash -n` for both shell launchers succeeded. Temporary-gitlink and fake scheduler/executable fixtures reject manifest/grid mutation and require exact producer-core equality. No RNG/Geant4/beam/detector population participated.
- **Local tooling boundary:** Ruff is not installed locally; no local Ruff PASS is claimed. Python sources were checked for >100-character lines after formatting. Protected exact-final-head MC Validation is required before integration.
- **Four-role pre-CI votes after adversarial correction:** detector-response/provenance lead `ACCEPT corrected source-binding design / REVISE pending CI / BLOCK #977 COMPLETE`; adversarial provenance reviewer `REVISE first draft, ACCEPT Git-object semantic check / BLOCK executable-byte identity`; independent validator `ACCEPT eight-test deterministic oracle / BLOCK detector inference / REVISE pending CI`; claims/provenance reviewer `KEEP #977/#1067 OPEN/PARTIAL`.
- **Important residual:** this binds campaign **source intent**, not executable bytes. A stale build can still differ from the manifest superproject commit. Direct/manual `sipm_sensitivity.py --expected-core-sha` remains an operator-controlled path unless separately required to consume the manifest. The job's verifier/helper and full launcher/build/toolchain bytes are not yet content-bound by this child.
- **Spawned children:** `ARU-SIPM-CAMPAIGN-POINT-MANIFEST-CONSUMER-001`; `ARU-SIPM-RUN-METADATA-BINARY-BUILD-MANIFEST-001`; `ARU-SIPM-CAMPAIGN-SUPERPROJECT-BUILD-CLOSURE-001`; `ARU-SIPM-CAMPAIGN-LAUNCHER-BYTE-BINDING-001`; `ARU-ELEC-IMPULSE-HISTORICAL-OUTPUT-AUDIT-001`; #1072; #1067.
- **Concurrent unrelated work:** draft PR #1279 remains DAQ S00 saturation authorization; do not absorb or overwrite it.
- **Next gate:** require both exact-final-head push and pull-request MC Validation contexts for #1284. If this source-intent child integrates, next highest-value atom is binary/build-manifest closure.
- **Scientific boundary:** no beam bytes, production Geant4 detector population, measured electronics calibration, DATA↔MC result, timing/PID metric, pile-up efficiency, rate, ESS, p-value, or detector-performance quantity was generated or promoted.
