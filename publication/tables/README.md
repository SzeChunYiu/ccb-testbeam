# Publication tables

`final/` contains only authorising paper tables. `gated/` contains historical/current tables that remain useful for review but are blocked from final publication.

The hardware BOM and canonical claim ledger remain source-of-truth files outside this folder; publication tables link to them rather than creating a second editable truth surface.
