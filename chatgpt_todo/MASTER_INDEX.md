# Master Review Index

| ID | Area | Type | Current state | Evidence / dependency | Next review action |
|---|---|---|---|---|---|
| IDX-G4-001 | `geant4/single_stave` multithread RNG ownership | Code / simulation | PARTIAL | PR #868 is closed without merge. Repository records report CI and GPU-node validation, but this session did not independently reproduce those runs or establish that every PR commit is present on `main`. | Map the validated implementation and artifacts to exact `main` commits; rerun required checks when the environment is available. |
| IDX-G4-002 | Single-stave optical-yield claim (~178 PE/event) | Study / claim | PARTIAL | Repository-recorded GPU-node result: mean 178.3 PE/event with 0.48% relative standard error across four seeds. Raw outputs and commands were not independently inspected in this session. | Verify immutable ROOT/JSON/PDF artifacts, hashes, event counts, configurations, and uncertainty calculation against exact `main` code. |
| IDX-G4-003 | Event-tree reproducibility validator | Code / plot | PARTIAL | Repository records report 27/27 branches exactly equal on real ROOT outputs. The underlying artifacts were not independently inspected in this session. | Link the validator command, ROOT hashes, JSON/PDF outputs, and exact `main` commit; independently reproduce when possible. |
| IDX-G4-004 | Photon-tree canonical multiset validator | Code / plot | PARTIAL | Repository records report 1,170,091 photon rows exactly equal. The underlying artifacts were not independently inspected in this session. | Link ROOT hashes, validator output, sensor/event-domain checks, and exact `main` implementation. |
| IDX-G4-005 | Multiseed RNG ensemble validator | Code / statistics | PARTIAL | Repository records report four seeds and 0.48% cross-seed relative standard error. Preregistered thresholds and artifacts were not independently inspected in this session. | Verify manifest, seed list, event counts, thresholds, JSON/PDF outputs, and exact `main` implementation. |
| IDX-G4-006 | PR #888 single-stave fix package and generated build products | Code / repository provenance | PARTIAL | PR #888 merged four source-level fixes plus 66 generated files under `geant4/single_stave/build/`. The build tree contained LUNARC-specific CMake paths, compiler probes, object files, an executable, and copied runtime assets. Cleanup commit `c7cdd653c5fef08b1e70cb33db9c574f7e7e0de9` removed the generated tree and added a recurrence test without reverting source changes. | Independently review each PR #888/#889 source claim, compile from a clean ignored build directory, run CTest/physics checks, and map any accepted result to immutable artifacts. |
| IDX-G4-007 | PR #890 PSTAR reference-path and self-test provenance | Code / reference data / validation | COMPLETE | The legacy default resolved above the repository and the inline fallback masked the error. Current `main` resolves the committed table, fails closed when absent, reports path/SHA-256/row count, labels output `DIAGNOSTIC_ONLY`, and has focused regression plus Markdown/JSON/SVG evidence. | Preserve the regression and provenance output when moving or replacing the reference table. |
| IDX-G4-008 | Single-stave stopping-power closure | Simulation / claim / plot | PARTIAL | The current comparison divides local unquenched deposited energy by scored path length and compares it with PSTAR at configured incident energy. This is a diagnostic proxy, not automatically projectile total energy loss; deuteron velocity scaling is approximate. | Use `G4EmCalculator` or a primary entry/exit-energy/path-length closure, quantify generated-secondary escape and energy evolution, separate proton direct reference from deuteron approximation, and retain immutable results and uncertainty plots. |
| IDX-G4-009 | PSTAR reference-energy domain and extrapolation behavior | Code / numerical method / validation | COMPLETE | The legacy interpolation silently clamped below/above-range lookups to the nearest endpoint. Current `main` rejects unsupported proton-equivalent energies, reports deuteron `E/2` lookup and table bounds, returns a nonzero CLI status, and has focused regression plus Markdown/JSON/SVG evidence. | Preserve fail-closed domain checks when changing interpolation, reference tables, or supported particle mappings. |
| IDX-G4-010 | PSTAR reference-table structural and numeric integrity | Code / reference data / validation | COMPLETE | The legacy parser silently skipped malformed rows, reordered surviving rows, and accepted duplicate, nonfinite, or nonphysical values. Current `main` validates every row, preserves declared order, requires finite physical values and strictly increasing energy, and returns input-error status 2 without numerical PASS. Focused regression and Markdown/JSON/SVG evidence are committed. | Preserve the strict parser when refreshing PSTAR data; separately verify source transcription and scientific reference provenance. |
| IDX-G4-011 | PSTAR diagnostic energy-deposit semantics | Code / physics convention / validation | COMPLETE | Legacy code allowed quenched visible-energy fields into the raw PSTAR tolerance gate after a warning. Current `main` rejects them by default, permits only labelled non-accepting proxy output, rejects mixed semantics, and records basis/comparability with focused regression plus Markdown/JSON/SVG evidence. | Preserve the raw-versus-quenched gate; accepted stopping-power closure still requires a validated projectile-energy-loss observable. |
| IDX-G4-012 | PSTAR diagnostic simulation-event input integrity and canonical integration | Code / schema / validation | COMPLETE | The former comparison reader silently omitted malformed rows and selected the first populated alias. Shared validator v1.2.0 validates and normalizes every row, and the canonical comparison now delegates to it. Malformed/ambiguous input returns status 2 before numerical PASS; output records input SHA-256, bytes, validated-row count, and validator version. Combined focused regression returned `35 passed`; integration Markdown/JSON/SVG evidence is committed. | Preserve one canonical parser; any future schema extension must update the shared parser and both standalone/integration regressions together. |
| IDX-G4-013 | Exact real stopping-power simulation-table validation | Data / simulation provenance | PARTIAL | Canonical parser integration is validated synthetically, but no exact real exported Geant4 event table was available in this run. | Run the integrated CLI on immutable real exports and retain exact path, bytes, SHA-256, validated rows, particle/energy coverage, deposit basis, code commit, command, output hash, and rejection state before interpreting any ratio. |
| IDX-G4-014 | PSTAR electronic+nuclear-to-total cross-column identity | Code / reference data / validation | COMPLETE | Exact-decimal validator v1.1.0 now returns canonical rows plus provenance, and `compare_stopping_power.py` invokes it directly. The exact committed blob `7e953dd...` remains 141/141 component-consistent; a mutated total returns status 2 with no output or numerical PASS. Combined focused regression returned `42 passed`; standalone and integration Markdown/JSON/SVG evidence is on `main`. | Preserve the single shared parser and exact-decimal identity gate; independently verify external NIST transcription/material provenance separately. |
| IDX-G4-015 | Configured-energy grouping before stopping-power comparison | Code / numerical method / validation | COMPLETE | The former aggregator rounded energies to 0.1 MeV, so synthetic 1.01 and 1.04 MeV rows silently became one pooled 1.025 MeV point. Current `main` groups by exact validated numeric energy, records `EXACT_CONFIGURED_ENERGY` in rows/CSV/CLI, and has an exact-old-blob regression plus `19 passed` focused validation and Markdown/JSON/SVG evidence. | Preserve exact grouping unless a scientifically justified binning rule, reference integration method, and sensitivity study are explicitly preregistered and validated. |
| IDX-G4-016 | Deuteron reference-basis authorization in the PSTAR diagnostic | Code / approximation / validation | COMPLETE | NIST PSTAR is a proton reference. The former code could accept a deuteron ratio using proton PSTAR at `E_d/2`. Current `main` rejects deuteron input by default and permits only labelled `VELOCITY_SCALED_PROTON_PROXY` output that is non-accepting; focused regression returned `9 passed` with Markdown/JSON/SVG evidence. | Preserve the fail-closed distinction. An accepted deuteron comparison requires an authoritative deuteron reference or separately validated approximation for the exact material and energy domain. |
| IDX-G4-017 | Uncertainty authorization for stopping-power tolerance claims | Code / statistics / validation | COMPLETE | The former direct-proton path could accept a one-event point estimate and print PASS without any uncertainty evaluation. Current `main` records `NOT_EVALUATED`, separates numerical proximity from acceptance, returns nonzero for point-estimate-only output, and retains an arithmetic-only self-test. Exact old bytes fail the new four-test regression; focused suite returned `19 passed`; Markdown/JSON/SVG evidence is committed. | Preserve fail-closed acceptance. Implement and preregister a complete uncertainty budget only together with an accepted projectile-energy-loss observable and immutable real exports. |
| IDX-G4-018 | Floating-point identity in stopping-power CSV and terminal reports | Code / serialization / validation | COMPLETE | Exact grouping retained `1.0000001` and `1.0000002` as distinct floats, but former `.6g` CSV output wrote both as `1` and terminal output printed both as `1.00`. Current `main` uses finite Python round-trip representations for every CSV float, records the serialization contract, and prints configured energy without precision collapse. Exact old bytes fail the new regression; focused tests returned `3 passed`; Markdown/JSON/SVG evidence is committed. | Preserve round-trip serialization for machine-readable outputs. Any future human formatting must remain explicitly noncanonical and must not replace exact CSV values. |
| IDX-G4-019 | Self-contained stopping-power central-value and classification reports | Code / numerical traceability / validation | COMPLETE | Former reports exposed only derived proxy/ratio/status and omitted deposited-energy sum, track-length sum, density, tolerance and estimator. Current result/CSV rows record all five, permitting exact central-value and point-estimate-gate reconstruction; exact old bytes fail the regression, focused suite returned `5 passed`, and Markdown/JSON/SVG evidence is committed. | Preserve these sufficient statistics and numerical settings in every machine-readable format. This does not replace real-export validation, uncertainty evaluation, or accepted projectile-loss closure. |
| IDX-G4-020 | Row-order invariance of stopping-power sufficient statistics | Code / numerical method / validation | COMPLETE | The former aggregator used repeated binary64 `+=`, so one validated synthetic event multiset yielded deposit sums `1.0` or `1.000000000000001` solely from row order. Current `main` uses `math.fsum` separately for deposit and track values, records `MATH_FSUM_PER_GROUP`, and has exact-old-blob negative control plus `8 passed` focused validation and Markdown/JSON/SVG evidence. | Preserve compensated grouped sums. Any future streaming implementation must prove permutation invariance or document and bound residual numerical sensitivity. |
| IDX-G4-021 | Byte-snapshot identity of canonical stopping-power simulation inputs | Code / provenance / validation | COMPLETE | Validator v1.2.0 reads exact bytes once, parses rows and derives byte count/SHA-256 from that same snapshot, records `SINGLE_READ_EXACT_BYTES`, and maps invalid UTF-8 to controlled status 2. Focused regression returned `19 passed`; the former algorithm fails both new tests; Markdown/JSON/SVG evidence is on `main`. | Preserve one-snapshot provenance. Validate exact real exports separately before interpreting any ratio. |
| IDX-G4-022 | Cross-energy stopping-power ratio summary | Code / statistics / reporting | COMPLETE | Current `main` removes the unsupported arithmetic mean, preserves exact-energy points, emits only descriptive min/max bounds labelled `no combined estimate`, and records `NO_CROSS_ENERGY_COMBINATION_WITHOUT_UNCERTAINTY_MODEL`. Focused audit/remediation regression returned `6 passed` with Markdown/JSON/SVG evidence. | Preserve separate point reporting until a preregistered combined measurand, uncertainty, covariance, weighting/likelihood, sensitivity, and coverage model is validated. |
| IDX-G4-023 | Stopping-power report output-path and publication safety | Code / provenance / artifact integrity | COMPLETE | Current source blob `043dbd8c...` rejects resolved and existing-file aliases to simulation/reference inputs, serializes to a unique same-directory temporary file, flushes/fsyncs, records bytes/SHA-256, and atomically publishes with `os.replace`. Direct CLI, symlink and injected-failure regressions plus report compatibility returned `12 passed`; AST audit is `VALIDATED`; Markdown/JSON/SVG evidence is on `main`. | Preserve the alias/atomic publication contract and final report provenance. Validate exact real exports separately before interpreting any stopping-power ratio. |
| IDX-G4-024 | Live executable-mapping provenance for compiled HIBEAM/Geant4 runtime | Code / runtime provenance | PARTIAL | PR #1204 is on protected `main@1b6608b8...`; exact-head MC Validation run `31450233217` is ruff-clean with `1511 passed, 1 skipped, 8 xfailed, 1 xpassed`; focused runtime-mapping suite `9 passed`. The tool binds final-build executable bytes to stable file-backed executable mappings, selected loader environment, and fail-closed hostile replacement/deletion/anonymous-exec controls. | Obtain a real immutable HIBEAM final-build receipt/PID and measure actual Geant4/VGM/ROOT mappings. Then close `ARU-MC-G4-LINK-METADATA-001`, late-`dlopen`, in-memory-page, wrapper, immutable-consumption, runtime-manifest and compiled physics-control children before authorising generator or detector claims. |
| IDX-I885-001 | Issue #885 proton/deuteron partial campaign coverage and legacy fit audit | Simulation / calibration / figures | COMPLETE | Manifest/result audit measured 14/72 total files, 14/40 main-grid files, and 7/20 independent energy points. The former summary used the wrong denominator and collapsed unequal species coverage; P5/P5b fitted per-seed rows while displaying seed averages. Validator, focused tests, corrected summary, quarantine, and Markdown/JSON/SVG evidence are on `main`. | Preserve coverage and independence checks when the campaign is extended. |
| IDX-I885-002 | Issue #885 seed-averaged response refit | Numerical method / simulation / plot | COMPLETE | Current `main` forms one point per energy, separates file and energy counts, combines propagated within-file and between-seed SEM, requires at least three energies, and records covariance/residual/chi-square diagnostics. Deuteron lines are skipped at two energies. Proton straight lines are rejected: reduced chi-square 357.99 for SiPM and 33391.66 for Birks-visible, both with 3 residual dof. No accepted fit remains. Focused regression returned `6 passed`; JSON/CSV/SVG/Markdown evidence is committed. | Preserve the empty accepted-fit state; do not publish the rejected diagnostic coefficients as calibration constants. |
| IDX-I885-003 | Issue #885 accepted response model and detector calibration | Simulation / model comparison / data closure | PARTIAL | The current partial campaign rejects the global proton line and cannot fit deuterons. No nonlinear/restricted-range model is preregistered or independently validated, and no real-data closure exists. | Complete or preregister an independent validation subset, compare physically plausible response models and restricted ranges using held-out/new energies, quantify systematic uncertainty and coverage, and perform data/MC closure before accepting calibration constants. |
| IDX-DOC-001 | Repository-local AI audit coordination | Documentation | ACTIVE | `chatgpt_todo/` exists and is being maintained, but stale and contradictory status entries have required correction. | Continue enforcing evidence classes, stable ownership, append-only provenance, and non-contradictory states. |
| IDX-WIKI-001 | GitHub wiki and repository `WIKI.md` | Documentation / claims | PARTIAL | Public C12 wording was synchronized in commit `d966384231ccf29b7e8e4f1563a46c281ca29782`; front-door MV4/tau/uncertainty wording is now synchronized, but remaining wiki claims have not been individually mapped to evidence. | Audit every remaining material wiki claim against code, data, simulation, literature, plots, and limitations. |
| IDX-WIKI-002 | WIKI front-door status, truth-type, and uncertainty governance | Documentation / claim provenance | COMPLETE | Five public statements were corrected; bound ledger rows `CL-007` and `CL-011` were restored to 43 columns; validator v1.1.0 handles summary rows and returns `VALIDATED` with zero issues; focused tests returned `5 passed`; 16 internal WIKI targets were validated; Markdown/JSON/SVG evidence is on `main`. | Preserve the validator and explicit uncertainty caveat when editing the front door or bound ledger claims. |
| IDX-LEDGER-001 | Claim-ledger row-width and field-alignment integrity | Documentation / schema / claim provenance | PARTIAL | The current schema record reports 26/26 exact-width rows and zero width issues. Exact width prevents shifted late-field interpretation but does not establish source support, correct values, uncertainty treatment, truth type, status, or downstream public wording. | Perform source-backed scientific review for every row and rerun downstream WIKI, claim, link, and figure checks before authorizing claims. |
| IDX-LEDGER-002 | Claim-ledger validation output safety | Code / provenance / artifact integrity | COMPLETE | Validator v1.1.0 rejects input/output and JSON/SVG aliases, uses same-directory temporary files with flush, `fsync`, and `os.replace`, preserves previous outputs on injected replacement failure, and cleans temporary files. Focused combined suite returned `19 passed`; Markdown/JSON/SVG evidence and immutable handoff are on `main`. | Preserve pairwise alias rejection and atomic publication whenever schema outputs or formats change. |
| IDX-P04P-001 | B2 duplicate-readout harm-veto model selection and `CL-015` | Study / model selection / claim | PARTIAL | The reported GBT coverage point `0.501643` barely exceeds the 0.50 gate while its run-bootstrap 95% interval `[0.478103,0.538255]` crosses it. The result does not state how uncertainty controls eligibility. A lower-bound sensitivity gate changes the first eligible method to MLP. `CL-015` is exact-width but `GATED`; audit/tests/JSON/SVG are on `main`. | Preregister an uncertainty-aware coverage rule and model-family multiplicity policy; use independent holdout/new runs; validate transfer to B4/B6/B8; preserve immutable rerun provenance before naming a production winner. |
| IDX-REPO-001 | Repository-wide study/code/data inventory | Repository | TRIAGED | Top-level categories and approximate counts are enumerated, but enumeration is not scientific review. Many areas below remain `NOT_STARTED`. | Build item-level inventories with stable IDs and evidence states; do not mark repository-wide audit complete until each item is reviewed. |
| IDX-STUDIES-001 | Study reports (reported count: 735) | Studies | NOT_STARTED | `reports/` directory count recorded by a prior session; individual reports are not represented one-by-one in this index. | Inventory each report with question, inputs, code, outputs, claims, evidence, and review state. |
| IDX-CONFIGS-001 | Study configurations (reported count: 367) | Configs | NOT_STARTED | `configs/` directory count recorded by a prior session; individual configs are not mapped to studies/results. | Cross-reference every configuration to study, code path, data provenance, output, and claim. |
| IDX-TESTS-001 | Test suite (reported count: 49) | Tests | ACTIVE | PR #884 identified two current-main amplitude-audit failures, corrected the stale assertion and aggregate counter, and passed MC Validation CI run `29993563323` before squash merge to `main` as `4f857f508160bbbe059d936866b426a45788c9bd`. A new Git-index regression now prevents tracked `geant4/**/build/**` artifacts. Repository-wide scientific acceptance coverage remains unaudited. | Continue inventorying tests by code/result dependency and require current-main checks for later changes; do not infer data, simulation, or detector validation from Python unit-test success. |
| IDX-CI-002 | Amplitude convention audit current-main gate | CI / audit code | COMPLETE | Exact two-file PR #884 patch reviewed; successful workflow run `29993563323`, job `89161772967`; remote-main merge commit `4f857f508160bbbe059d936866b426a45788c9bd`; post-merge files re-read. | Preserve regression coverage when changing warning names, baseline-quality states, or convention-specific aggregate counters. |
| IDX-SCRIPTS-001 | Analysis scripts (reported count: 651) | Scripts | NOT_STARTED | Script count recorded by a prior session; individual scripts are not audited for correctness or reproducibility. | Audit each entry point, inputs, outputs, units, determinism, failure behavior, and downstream claims. |
| IDX-NOTEBOOKS-001 | Jupyter notebooks (reported count: 3) | Notebooks | NOT_STARTED | Notebook count recorded; outputs and execution order are not validated. | Execute from clean environments where possible and compare generated outputs with reported claims. |
| IDX-ANOM-001 | C12 anomaly transfer from MC to data | Study / claim | PARTIAL | Public wording is corrected to `TRUTH_LEVEL_MC_ONLY`, but the matched data/MC closure specified in `docs/validation/C12_DATA_MC_CLOSURE_SPEC.md` has not been executed. | Perform the frozen cross-domain closure with counts, Wilson intervals, morphology/rate comparison, purity/efficiency, stability tests, hashes, JSON, and plots. |
| IDX-AMP-001 | Legacy `amplitude_adc` convention authorization | Code / data contract | PARTIAL | Tooling binds the exact input-table hash to measured supporting-artifact bytes and verifies optional line ranges. Focused synthetic regression passed, but no real A-002 bytes or evidence artifact were available. | Build a real hash-bound A-002 evidence map, cite exact supporting lines or the whole immutable file, run the full-table convention audit, and review every warning/error. |
| IDX-DELTAE-001 | A-002 absolute ADC polarity and stopping reconstruction | Code / result / plot | PARTIAL | The bridge previously used `abs(amplitude_adc - baseline_adc)`, which erased pulse polarity. Current code requires explicit positive/negative polarity, signed subtraction, and fail-closed rejection of opposite-polarity or nonfinite rows. Exact A-002 bytes and polarity evidence remain unavailable. | Obtain hash-bound convention and polarity evidence, rerun the bridge, verify one row per physical event, and regenerate the result JSON, event CSV, stopping counts/fractions, and ΔE–E plot. |
| IDX-FIGURES-001 | Figure registry and generated figures | Visualization | NOT_STARTED | `paper/figures.yaml` and figure tooling exist; individual figures are not audited. | Verify every figure's input hashes, command, selection, normalization, uncertainty, axes, caption, and claim linkage. |
| IDX-TOOLS-001 | Audit/provenance/figure tools | Code | ACTIVE | Tool packages are enumerated but not comprehensively reviewed. | Test functionality, failure modes, schema compatibility, determinism, and documentation. |
| IDX-FLEET-001 | LUNARC fleet configurations | Infrastructure | NOT_STARTED | `fleet/` is enumerated but execution environment and pipeline are not fully documented. | Document environments, software versions, job scripts, artifact transfer, and reproducibility controls. |
| IDX-DATA-001 | Data files and external artifacts | Data | NOT_STARTED | `DATA.md` and artifact locations are enumerated; provenance and integrity are not comprehensively verified. | Inventory datasets with source, schema, hashes, access path, selection, calibration, exclusions, and downstream use. |

## Completion gate

Repository-wide completion requires every identifiable study, material claim, major code path, dataset/simulation, figure/table, wiki page, and documentation area to have an item-level stable ID and an evidence-backed review state. Directory counts or top-level enumeration alone are `TRIAGED`, not `COMPLETE`.

This index is cumulative. Add one row for every identifiable study, code area, dataset, simulation, material claim, figure, table, wiki page, and documentation area.
