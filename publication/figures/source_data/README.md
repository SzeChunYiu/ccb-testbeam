# Figure source data

Do not duplicate large result tables here. Final figure manifests should point to the canonical result/source-table paths and SHA-256 values in the repository. A publication build should be reproducible from those immutable artifacts.
