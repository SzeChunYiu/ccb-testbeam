# Active Task

- **Task ID:** `ARU-MC-G4-LOADER-ARGV-001`
- **Owner:** hourly Atomic Research Universe audit session
- **Protected main inspected:** `41a568a7296ac947e9ecb5baf540b0505c0edad1`, the squash merge of validated PR #1211. #1182 and CL-021 remain gated.
- **Branch:** `audit/geant4-loader-argv`, based exactly on that protected main.
- **Selected universe:** exact scientific meaning of the Linux `/proc/<pid>/cmdline` bytes as a child of `ARU-MC-G4-LOADER-SEARCH-001` under #1182.
- **Exact contract:** compose a PASS `ccb_geant4_runtime_dependency_attestation_v1`; require the same `(pid,starttime_ticks,exe_link)` before/after; read `/proc/<pid>/cmdline` twice; require byte equality; preserve every NUL-delimited argument slot exactly, including empty/non-UTF8 slots; serialize raw/per-argument byte counts, SHA-256 and base64; self-digest the receipt.
- **Scientific interpretation:** this proves only a stable process-visible argument region at the attestation boundary. Linux permits post-exec argv-string rewriting and `PR_SET_MM_ARG_START/END`, so a late stable snapshot is not immutable historical `execve(argv)` evidence. `argv[0]` is not executable identity; the parent content-bound `/proc/<pid>/exe` observation remains separate.
- **Repository front door:** `geant4/setup_and_run.sh` invokes `./hibeam_g4 -c krakow.config -m run_krakow.mac output_krakow.root`; therefore exact initial cwd and consumed input bytes are material children before those relative spellings acquire file identity.
- **Mechanisms considered:** direct target exec; arbitrary/mutated argv0; explicit glibc dynamic-loader invocation; post-exec argv rewrite; `PR_SET_MM_ARG_START/END`; separate `PR_SET_MM_EXE_FILE` redirection.
- **Executed mechanism controls:** local C child rewrote argv0 and `/proc/<pid>/cmdline` changed while `/proc/<pid>/exe` stayed on the program; explicit glibc-loader launch exposed loader identity in `/proc/<pid>/exe` and loader+target in cmdline. These are Linux mechanism falsifiers, not HIBEAM validation.
- **Local deterministic execution:** Python 3.13.5/Linux/no RNG, focused pytest -> `11 passed in 1.40s`; `py_compile` passed. Local ruff unavailable.
- **Exact content binding:** tool 9263 bytes, SHA-256 `4d1a8a195b10d40303dfab9bf8aac9605969134e65f8c114ea54dddb870cfba3`, Git blob `80e51cfe45037f95cc86039506a7dcee648f4474`; tests 9259 bytes, SHA-256 `3de0b2d5d736f2e5cb38034a5fbe848ae2790be2382861ae454525c1b85546b0`, Git blob `6d2a962b329cf6df9c2b08f08d22473733dfea1d`.
- **Four-role state:** runtime/physics lead `ACCEPT bounded observation / REVISE historical invocation`; adversarial Linux/loader reviewer `ACCEPT discriminator / BLOCK historical direct-exec claim`; independent validation reviewer `ACCEPT deterministic oracle / BLOCK HIBEAM and physics inference`; claims/provenance reviewer `ACCEPT provenance refinement / BLOCK CL-021 promotion`.
- **Children:** `ARU-MC-G4-LOADER-INITIAL-CWD-001`, `ARU-MC-G4-LOADER-ARGV-REGION-MUTATION-001`, `ARU-MC-G4-LOADER-EXE-REDIRECTION-001`, `ARU-MC-G4-RUNTIME-ARGUMENT-SEMANTICS-001`; existing cache/config, token/hwcaps, preload/audit, linker/static, late-dlopen, wrapper, immutable-consumption, runtime-manifest, source/stopping, weights and detector-response children remain open.
- **Repository files:** new attestor, hostile tests, curated ruff-list integration, and immutable archive `chatgpt_todo/archive/2026-08-11T105100Z_ARU-MC-G4-LOADER-ARGV-001.md` are on the branch.
- **Concurrent CI note:** unrelated PR #1212 currently fails its enforcement gate only because its newly exposed base-freshness tool/test have two ruff I001 import-order findings; its full pytest run passed `1594 passed, 1 skipped, 8 xfailed, 1 xpassed`. Do not merge or borrow that stale green pytest as authorization for this branch.
- **CI/merge gate:** require fresh exact-final-head MC Validation with curated ruff + full non-integration pytest + diagnostics/enforcement and recheck current-main ancestry before merge. No production HIBEAM runtime receipt exists in this session.
- **Status:** `ACTIVE / IMPLEMENTED_ON_BRANCH / LOCAL_FALSIFIERS_PASS / EXACT_BLOB_BINDING_CONFIRMED / EXACT_HEAD_CI_REQUIRED / REAL_HIBEAM_RUNTIME_BLOCKED / PHYSICS_INFERENCE_BLOCKED`
