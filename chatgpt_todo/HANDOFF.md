# Latest Handoff

## SiPM campaign source intent survives adversarial source/output review; CI fixture corrected after exact-head failure

Selected atom `ARU-SIPM-CAMPAIGN-MANIFEST-CORE-SHA-SOURCE-001` remains `ACTIVE / PRE-CI` in draft PR #1284 under open parent #977. The branch started from protected `main@4abbf9112ad7933b4f0ec5b927f2d0f358ad08be`, which pins `geant4/single_stave/sipm` to exact `ccb-sipm-core@3627dc87137a9f33f511a755671414b11853c0a0`. Base main-push MC Validation run `31565791869` is confirmed `SUCCESS`.

The source-binding design remains: canonical `ccb-sipm-campaign-intent/1` bytes derive expected core from the recorded superproject commit, hash every selected points grid, freeze base CLI/event-count/thread intent, copy the manifest digest into every Slurm argv, verify manifest/grid/source binding before simulation, and require produced compile-bound sidecar core SHA to equal campaign intent before point provenance is written. The first adversarial correction replaced a descriptive `SUPERPROJECT_GITLINK:...` label with semantic `git ls-tree <recorded_commit> geant4/single_stave/sipm` verification.

A second cross-atom review found that the first launcher generated into tracked `geant4/single_stave/slurm/grids/` and then required a fully clean worktree, so any documented `CCB_GRID_*` override dirtied tracked `points_*.csv` before manifest creation. A temporary-Git discriminator reproduced ` M geant4/single_stave/slurm/grids/points_test.csv`. The repaired launcher writes campaign-specific grids to `${CCB_CAMPASSIGN_OUTDIR}/grids` outside the repository, hashes those exact bytes into the manifest, submits those same files, and analyzes those same files; repository-local campaign output is rejected so the all-files clean-source gate remains strong.

Exact-head pull-request MC Validation `31568703445`, job `94025888229`, on superseded head `4535a7742b3e45ed60ad7fc862ff9e5c0d2ed0cb` then exposed a **test-harness**, not production-law, defect. Core compile/CTest was green (7/7), curated ruff was green, but the launcher fixture assumed the enclosing CI checkout must be clean at pytest start. In reality editable installation, prior Python tests and workflow diagnostics had already modified tracked bytecode/egg-info and created `sipm_core.log`, `ruff.log`, `pytest.log`. The suite returned `1 failed, 2134 passed, 2 skipped, 8 xfailed, 1 xpassed, 18 warnings in 140.84s`; enforcement recorded `SIPM_CORE_STATUS=0`, `RUFF_STATUS=0`, `PYTEST_STATUS=1` and correctly failed.

That failure is preserved at `chatgpt_todo/archive/2026-08-12T061300Z_ARU-SIPM-CAMPAIGN-MANIFEST-CORE-SHA-SOURCE-001_CI-FIXTURE-CORRECTION.md`. The repair does **not** weaken `require_clean_worktree()`. The integration regression now creates a fresh detached exact-HEAD Git worktree, verifies that isolated source is clean, runs the actual launcher there with fake `sbatch` and `CCB_GRID_PDE_SCALE="0.95 1.05"`, verifies external generated-grid SHA-256 binding plus unchanged isolated-worktree status, and removes the worktree. CI dirt in the enclosing checkout is no longer conflated with production source provenance.

Four sequential votes after the exact-head falsifier: detector-response/provenance lead `ACCEPT production clean-source invariant / REVISE test harness pending CI / BLOCK #977 COMPLETE`; adversarial reviewer `REJECT weakening require_clean_worktree / ACCEPT isolated exact-HEAD fixture`; independent validator `ACCEPT failed CI as informative falsifier / REVISE pending exact-head rerun / BLOCK detector inference`; claims/provenance reviewer `KEEP #977 and #1067 OPEN/PARTIAL`.

Material residuals remain. Source intent and campaign-grid hashes do not prove which `ccb_stave_sim` binary/toolchain generated an output. A stale build may still disagree with the manifest superproject commit. Direct/manual `sipm_sensitivity.py --expected-core-sha` remains operator-controlled unless separately manifest-bound. The Slurm job still invokes helper/verifier bytes from a shared checkout rather than a content-addressed execution bundle. Historical pre-#1280 sidecars remain unauthenticated. Retain `ARU-SIPM-CAMPAIGN-POINT-MANIFEST-CONSUMER-001`, `ARU-SIPM-RUN-METADATA-BINARY-BUILD-MANIFEST-001`, `ARU-SIPM-CAMPAIGN-SUPERPROJECT-BUILD-CLOSURE-001`, `ARU-SIPM-CAMPAIGN-LAUNCHER-BYTE-BINDING-001`, `ARU-ELEC-IMPULSE-HISTORICAL-OUTPUT-AUDIT-001`, #1072 and #1067.

Draft PR #1284 remains the only intended integration vehicle; unrelated draft PR #1279 remains DAQ saturation work. The failed head is not merge-authorising. Require both push and pull-request MC Validation on the new exact final head before marking ready or merging. No beam bytes, production Geant4 campaign, measured electronics calibration, timing/PID result, rate, ESS, p-value, or detector-performance claim was produced or promoted.
