# Active Task

- **Task ID:** `ARU-SIPM-PREWINDOW-DARK-HISTORY-001`, continuation leaf under reopened #1096.
- **Owner:** hourly Atomic Research Universe audit session.
- **Protected main at selection:** `fcb246a1442d8ab9aa5fee8bce2337f46749dd06`, merge of #1223. This points `geant4/single_stave/sipm` to `ccb-sipm-core@2027b06e0fb47b26da1b89e95b6901a5f8e6c200`.
- **Selected contract:** the explicit physical history interval and waveform sample interval are distinct. Under the core's homogeneous DCR model, dark primaries must cover `[history_start_ns, window_end_ns]` when explicit history simulation is used, unless a separately validated sufficient-state or physical-gate model replaces that contract.
- **Confirmed contradiction:** `schedule()` now accepts candidates from `history_start_ns`, but `ResponseSimulator::simulate()` still generates dark counts with duration `window_end_ns-window_start_ns` and uniform times only in `[window_start_ns,window_end_ns]`. Supplied pre-window photons can influence the sample; spontaneous dark primaries cannot.
- **Default model scale:** `history_start=-200 ns`, `window_start=-20 ns`, `window_end=250 ns`, DCR `500 kHz`; omitted prehistory is 180 ns, with model expectation `E[N_missing]=0.09` and `P(N_missing>=1)=0.0860688147` per sensor/event. The omitted interval is 40% of the history-inclusive dark-primary measure (`0.09/0.225`). These are simulator-model expectations, not detector measurements.
- **Test gap:** upstream Task-C C1-C5 use a helper that disables DCR/crosstalk/afterpulse and prove only a supplied photon at `-21 ns` contributes. ccb-testbeam MC Validation does not initialize/build/test the submodule; #1223's green Python/static workflow therefore is not an independent C++ execution of the dependency.
- **Parent state:** #1096 was reopened because its own acceptance criteria require cross-window recovery/correlated-noise memory and history-length convergence, which remain unclosed.
- **Four-role state:** SiPM/electronics lead `REVISE parent closure`; adversarial stochastic-process reviewer `BLOCK COMPLETE`; independent validation reviewer `ACCEPT control-flow diagnosis / BLOCK patched-core validation`; claims/provenance reviewer `BLOCK #1096 COMPLETE / no detector-claim promotion`.
- **Preferred bounded repair:** in ccb-sipm-core, generate the DCR Poisson mean and dark-time support from `history_start_ns`; add a deterministic history-support regression plus stochastic sanity control; compile/test the exact core SHA before updating the ccb-testbeam gitlink.
- **Spawned child:** `ARU-SIPM-HISTORY-HORIZON-CONVERGENCE-001`. The 180 ns explicit prehistory is 7.2× the default 25 ns decay relative to the sample start, while recovery and the configured slow afterpulse (`tau=80 ns`) carry longer stochastic memory. Choose history horizon by convergence of declared in-window observables/state, not one analog decay constant.
- **Archive:** `chatgpt_todo/archive/2026-08-11T165700Z_ARU-SIPM-PREWINDOW-DARK-HISTORY-001.md`.
- **Branch:** `audit/sipm-prewindow-dark-history`, base `main@fcb246a...`.
- **Scientific boundary:** no production Geant4 campaign, beam/production-MC ROOT bytes, measured DCR, detector baseline, timing, pile-up, PID, rate, ESS, p-value, or detector-performance result was regenerated or promoted.
- **Next highest-value atom:** implement and execute the core DCR-history regression; then run `ARU-SIPM-HISTORY-HORIZON-CONVERGENCE-001`. Independently, the Geant4 provenance lane still needs actual relative-input consumption/open-byte closure after merged #1222.
- **Status:** `ACTIVE / #1096_REOPENED / DARK_HISTORY_SUPPORT_GAP_CONFIRMED / CORE_PATCH_NOT_EXECUTED / HISTORY_HORIZON_CHILD_OPEN / DETECTOR_CLAIMS_GATED`.
