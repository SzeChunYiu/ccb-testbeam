# Active Task

- **Selected atom:** `ARU-TIMING-CFD-NOISE-PHASE-SATURATION-SENSITIVITY-001` is `PARTIAL`, child of #1059/#1063 and stacked on selector-identifiability PR #1274.
- **Protected main inspected:** `main@ac2e0bdd873016531f9ef31b30048275c3d2965d`, protected with required `test` context. Parent #1274 was refreshed non-force onto that exact main as head `06618a7ab7b3836b0c7a0e7e0160c88842eee2f9`; its fresh push/pull_request checks must pass before parent integration.
- **Governance repair:** #1059 had drifted to `closed/completed` although its real-data transition, ambiguity, truth-transfer and downstream-regeneration acceptance criteria remain unresolved. It was reopened. Merged software/ADR state is not scientific completion.
- **Beam-data blocker:** immutable HRD ROOT bytes are external to GitHub at `/projects/hep/fs10/shared/nnbar/ccb_data/hrd/root` and are unavailable in this execution environment. The checked-in timing result is explicitly quarantined legacy output, so no new beam metric is inferred from it.
- **Atomic additive law:** for selected local candidate `j`, `F=alpha*max(y)`. Under arbitrary `||delta||_inf<eps`, sufficient persistence radii are `(y[j]-F)/(1+alpha)` for floor eligibility and half the two neighbour-order margins. Each earlier ineligible sample receives the strongest certificate among its currently failed predicates; the exact-selected-index sufficient radius is the minimum across selected eligibility and earlier exclusion. Guarantee is strict and non-authorising.
- **Near-floor discriminator:** `[0,25,49.9,25,0,0,500,1000,500]`, `alpha=.05`, switches under adversarial `+eps` early / `-eps` global at exact `eps*=0.1/1.05=0.09523809523809524 ADC`.
- **Baseline discriminator:** on `[0,20,40,20,0,0,500,1000,500]`, a common residual offset changes floor margin as `m'(b)=m+(1-alpha)b`; selector switches at `b*=10/(.95)=10.526315789473685 ADC`.
- **Clipping discriminator:** same fixture with `min(y,C)` retargets the unchanged 40-ADC early component when `C<=800 ADC`; tests distinguish `C=801` from `C=800`.
- **Sampling-phase discriminator:** synthetic separated triangular fixture sampled at `n+phi`; examples select late/early/late/late at `phi=0/.2/.5/.8`. Exhaustive deterministic 1001-point phase grid yields indices `{3:229, 9:300, 10:472}`. These are fixture-support counts, not a detector probability.
- **Controls:** clean `[0,50,100,50,0]` has 25-ADC sufficient exact-index radius; a 100/100/100 plateau has zero exact-index certificate; monotonic fallback has 5-ADC certificate.
- **Implementation:** stacked branch `audit/cfd-selector-nuisance-sensitivity-v1` adds `scripts/cfd_selector_sensitivity.py`, focused tests, immutable archive, and coordination updates. No RNG or toy-MC detector claim.
- **Four-role votes:** timing lead `ACCEPT deterministic sensitivity law / BLOCK detector-stability inference`; adversarial DAQ reviewer `ACCEPT estimator counterexamples / BLOCK occurrence/mechanism claims`; independent validation `ACCEPT deterministic oracle / REJECT probabilistic interpretation`; claims/provenance `ACCEPT bounded child / KEEP #1059 OPEN/PARTIAL`.
- **Next physical children:** real-data transition matrix; measured baseline-residual distribution/covariance; actual DAQ clipping/transfer closure; sampling-phase/aperture distribution; held-out truth transfer.
- **Claim boundary:** no beam timing resolution, pile-up/saturation rate or mechanism, WLS response, PID, efficiency, rate, ESS, p-value or detector-performance quantity is regenerated or promoted.
- **Status:** `DETERMINISTIC_NUISANCE_SENSITIVITY_IMPLEMENTED / EXACT_HEAD_CI_REQUIRED / RAW_BEAM_BYTES_BLOCKED / #1059_REOPENED_PARTIAL / TIMING_CLAIMS_GATED`.
