# Active Task

- **Task ID:** `ARU-MC-G4-LOADER-ARGV-001`, with active validation child `ARU-MC-G4-LOADER-ARGV-TEST-EXEC-RACE-001`.
- **Owner:** hourly Atomic Research Universe audit session.
- **Protected main / base:** `69678659797d9112a92f911b3011a4411393c1eb` via merged #1212. PR #1213 is based on this exact protected head and remains draft while fresh final-head checks run.
- **Branch / PR:** `audit/geant4-loader-argv` / #1213. #1182 and CL-021 remain gated.
- **Parent scientific universe:** exact meaning of Linux `/proc/<pid>/cmdline` bytes. The implemented attestor composes a PASS runtime-dependency receipt, requires stable `(pid,starttime_ticks,exe_link)`, reads cmdline twice byte-exactly, preserves every NUL-delimited slot, and self-digests the receipt. This proves only a stable process-visible argument region at the observation boundary; it is not immutable `execve(argv)` evidence.
- **Repository front door:** `geant4/setup_and_run.sh` uses relative `./hibeam_g4 -c krakow.config -m run_krakow.mac output_krakow.root`, so initial cwd and exact consumed input bytes remain material children.
- **Previously executed mechanism controls:** argv0 rewrite and explicit glibc-loader invocation falsified the hypotheses that cmdline is immutable launch argv or that argv0 is executable identity. No HIBEAM production process participated.
- **Exact-head CI falsifier discovered this run:** head `efa67b0ea2849ffc3d041e97487f74953e96f340` produced two MC Validation `test` checks. PR run `31485819366` succeeded, but push run `31485815692` failed with `1 failed, 1604 passed, 1 skipped, 8 xfailed, 1 xpassed`. The sole failure was `test_real_linux_child_observation_is_stable`, where the fixture waited only for `/proc/<pid>/cmdline` path existence and then observed an empty byte string before its intended `/bin/sleep` procfs exec state was ready. The production attestor correctly failed closed on the empty region.
- **Repair:** commit `52c0496396343613f1833b42e92fa3d0b7f4daec` adds a bounded state-based `_wait_for_exec_observation`: child alive, nonempty cmdline, exact expected `/proc/<pid>/exe` target, readable starttime. Both real-process tests use it. The production attestor is unchanged. New test Git blob: `d312d54cab63166c5f4b5b958f59c6da015fb4e2`.
- **Immutable continuation record:** `chatgpt_todo/archive/2026-08-11T112300Z_ARU-MC-G4-LOADER-ARGV-TEST-EXEC-RACE-001.md`.
- **Four-role state:** runtime/physics lead `ACCEPT repair / BLOCK physics inference`; adversarial Linux reviewer `ACCEPT state-based wait / REJECT timing-only workaround`; independent validation reviewer `REVISE until both fresh exact-head workflow contexts pass`; claims/provenance reviewer `ACCEPT fixture-provenance correction / BLOCK CL-021 promotion`.
- **Children after parent closure:** `ARU-MC-G4-LOADER-INITIAL-CWD-001` remains the highest-value scientific child; also `ARU-MC-G4-LOADER-ARGV-REGION-MUTATION-001`, `ARU-MC-G4-LOADER-EXE-REDIRECTION-001`, `ARU-MC-G4-RUNTIME-ARGUMENT-SEMANTICS-001`, plus cache/config, token/hwcaps, preload/audit, linker/static, late-dlopen, wrapper, immutable-consumption, runtime-manifest, source/stopping, weights and detector-response atoms.
- **CI/merge gate:** require the final branch head to retain exact protected-main ancestry and require every exact-head `test` check, including push and pull-request MC Validation runs, to finish green. Curated ruff, full non-integration pytest, diagnostics upload and enforcement must all succeed. One green duplicate context is not sufficient if another exact-head context fails.
- **Status:** `ACTIVE / REPAIRED_ON_BRANCH / EXACT_HEAD_CI_REQUIRED / REAL_HIBEAM_RUNTIME_BLOCKED / PHYSICS_INFERENCE_BLOCKED`.
