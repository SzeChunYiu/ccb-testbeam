% Internal collaboration-review wrapper.
% Defining this flag before loading main.tex enables real rendering of existing
% gated artifacts while preserving the canonical submission build unchanged.
\def\RoughPaperFigures{1}
\input{main.tex}
