# Active Task

- **Completed selected atom:** `ARU-ELEC-IMPULSE-DIGEST-RUNTIME-BINDING-001` is `VALIDATED` at bounded software/provenance-object level and integrated on protected testbeam main. Parent #1067 remains `OPEN/PARTIAL`.
- **Protected main transition:** exact pre-integration main `594bea0807e53d5f3e55a2b2e29bd85f82aa1f3e` advanced through #1234 to `e25d59be37f59b8fddf7dd897295bcaa7bee14d0`. Exact merged tree binds `geant4/single_stave/sipm -> ccb-sipm-core@66cc250f837be5bd30f8bec9bd67b8ec9ca9e7ed`.
- **Core runtime-object contract:** `N_out=floor((window_end-window_start)/dt)+1`, `N_hist=ceil(max(0,window_start-history_start)/dt)`, `N_kernel=N_out+N_hist`. A waveform-producing simulator constructs one private history-complete peak-normalised kernel object `K`; convolution consumes `K` by const reference and metadata records `CanonicalEffectiveKernelHash(dt,K)`. Caller-supplied effective hashes are overwritten. With `generate_waveform=false`, no consumed runtime kernel exists and the effective hash is empty.
- **Waveform law preserved:** `V_i=sum_a A_a h(t_i-t_a)` for causal `t_i>=t_a`, using the existing continuous fractional-delay interpolation. This atom changes provenance/object lifetime, not the numerical detector-response law.
- **Equivalence collapse:** positive global source-amplitude scale is intentionally invisible to the effective hash after peak normalisation; source/sample identities remain distinct. Shape, `dt`, and exact history-complete support length are identity-bearing.
- **Core execution:** core PR #12 final head `e50ead854430a41c02bf40c1bf1db8955a9fe10a`; initial CI `31530401219` preserved a stale-oracle contradiction while the new runtime-binding test passed. Repaired final exact-head Core CI `31530628915` / job `93909520107` passed configure/build and `7/7` CTest targets. PR #12 squash-merged as exact core main `66cc250f...`; independent main-push Core CI `31530776448` also completed SUCCESS.
- **Testbeam integration:** #1234 exact head `2ac64e167941d3eddfe0a7cee62c834e9f061360` passed both exact-head MC Validation contexts before merge: push `31531012658` / job `93910772484` and pull-request `31531115273` / job `93911094601`; both completed checkout, setup, install, ruff, unit tests, diagnostics and enforcement successfully. #1234 then squash-merged with expected-head guard as `e25d59be...`.
- **Post-merge validation:** main-push MC Validation `31531638356` is attached to exact merged main `e25d59be...`; it was queued at the coordination update point, so no post-merge PASS is claimed here yet.
- **Governance correction:** #1087's squash message carried historical text `fixes #1067`, which auto-closed P0 #1067 despite explicit PARTIAL coordination and unresolved children. #1067 was reopened, and #1218 now records this as a second live regression witness. #1234 used non-closing merge text and explicitly preserved #1067 OPEN/PARTIAL.
- **New cross-scale child:** source inspection after integration spawned `ARU-ELEC-IMPULSE-RUN-METADATA-SERIALIZATION-001`. Current `EventAction.cc` constructs a fresh `ResponseSimulator` for every event/sensor and calls `simulate(...)`, while `RunAction::WriteMetadataSidecar()` writes the production `<output>.meta.json` without serializing core `RunMetadata`, `impulse_response_status`, canonical `effective_kernel_hash`, or exact core gitlink. Thus the micro-level exact runtime identity is not yet proven to propagate into the run artifact.
- **Next atom contract:** for every event/sensor contributing persisted ADC, the serialized run/per-sensor metadata must bind the exact waveform-affecting configuration and canonical kernel identity used by that simulator, plus exact core gitlink/code identity. A single run-level identity is valid only if all waveform-affecting per-sensor/event configurations are proven identical; otherwise serialize variants separately. Preserve `CUSTOM_UNVALIDATED` versus future authorized `MEASURED` state.
- **Required next falsifiers:** config-A metadata with config-B simulation; forged effective hash; sensor-specific waveform override; sensor-id-only equivalence control; no-waveform state; provenance-status laundering; wrong core SHA; sidecar publication failure/alias; reopen-and-validate exact output sidecar.
- **Scientific boundary:** exact runtime-kernel identity is necessary provenance, not measured calibration. External source bytes/parser binding, physical units/polarity/baseline/time-zero/normalisation/resampling validation, positive typed `CUSTOM_UNVALIDATED -> MEASURED` authorization, historical-output audit, and #1096 physical history-horizon convergence remain unresolved. No detector/public claim is promoted.
- **Four-role votes on completed atom:** electronics/calibration lead `ACCEPT runtime-object binding / REVISE measured-calibration authority`; adversarial reviewer `ACCEPT fail-closed same-object identity / REJECT caller-hash authority`; independent validation reviewer `ACCEPT deterministic C++ oracle / BLOCK detector inference`; claims/provenance reviewer `ACCEPT bounded provenance advance / BLOCK #1067 COMPLETE and measured-electronics/public claims`.
- **Archive:** `chatgpt_todo/archive/2026-08-11T200100Z_ARU-ELEC-IMPULSE-DIGEST-RUNTIME-BINDING-001.md`.
- **Status:** `RUNTIME_BINDING_INTEGRATED_ON_MAIN / POSTMERGE_TESTBEAM_CI_PENDING / #1067_OPEN_PARTIAL / RUN_METADATA_SERIALIZATION_NEXT / #1218_OPEN_SECOND_REGRESSION_WITNESS / SOURCE_BYTES_AND_CALIBRATION_OPEN / #1096_HISTORY_HORIZON_OPEN / DETECTOR_CLAIMS_GATED`.
