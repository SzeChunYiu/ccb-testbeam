# Chapterized TeX source

`main.tex` is intentionally thin. Each numbered manuscript section is edited in one file here so physics reviewers can diff and review sections independently. Appendices `A_*` and `B_*` keep publication readiness and evidence provenance inside exported PDFs.

Do not copy content back from the historical `docs/latex/` report without re-validating the claim status.
