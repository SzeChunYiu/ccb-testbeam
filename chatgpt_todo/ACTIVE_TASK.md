# Active Task

- **Selected atom:** `ARU-TIMING-CFD-COMPONENT-CROSSING-BINDING-001` is `PARTIAL`, child of #1059/#1063. Post-#1239 review found that `first_local_peak` changed the CFD threshold amplitude to a selected local peak but still searched the whole waveform for the first threshold crossing, so the amplitude-defining component and time-defining component could differ.
- **Protected main inspected:** `main@ed1cea48e3b68bad8c30b7970cb2355cef94d02d`, merge of Lane05 Wave-A PR #1239. Open #1059 remains the P0 component-switch universe; #968 remains microscopic late-component/pile-up discrimination.
- **Atomic contract:** selected peak `(p,A_p)` with `T=f*A_p` must use the nearest pre-peak bracket `y[k] < T <= y[k+1]`, `k+1<=p`, with linear interpolation. If no pre-peak sample is below `T`, the selected rise is left-censored. Earlier above-threshold activity does not imply censoring if the trace subsequently falls below `T` before the selected rise. `global_max` keeps its historical first-crossing semantics as a separate estimator.
- **Primary falsifier:** `[0,40,0,50,100,50,0,0,500,1000,500]`; 5%-of-global selector rejects the 40-ADC bump, selects 100 ADC at sample 4, and CFD20 has `T=20`. Old code returned `t=0.5` from the rejected bump; component-bound crossing is `t=2.4` from samples 2→3.
- **Adversarial revision:** first repair draft incorrectly retained global sample-0 censoring. `[30,0,50,100,50,0,250,500,250]` has sample 0 above the selected peak's CFD20 threshold but returns below threshold before the selected rise, so that rise is observed at `t=1.4`, not left-censored. The branch was revised accordingly.
- **Implementation:** branch `audit/cfd-component-bound-crossing`; `scripts/digital_cfd.py` now carries peak index with the selected amplitude and binds the crossing to that component. `tests/test_cfd_component_binding.py` contains rejected-bump, component-relative censoring, clean-single-pulse, genuine-left-censor, and global-estimator non-regression controls. Draft PR #1250 remains unmerged.
- **Issue governance:** stable concern `CCB-1059-COMPONENT-CROSSING-BINDING-001` added to existing #1059; no duplicate issue opened. #1059 stays OPEN because real-data transition decomposition, ambiguous-component handling and held-out truth transfer remain unmet.
- **Four-role votes:** timing/estimator lead `ACCEPT same-component contract / BLOCK detector-truth interpretation`; adversarial waveform reviewer `REVISE first draft`, then `ACCEPT corrected bounded semantics conditional on CI`; independent validation reviewer `ACCEPT deterministic oracle / BLOCK timing-resolution inference`; claims/provenance reviewer `ACCEPT bounded repair / BLOCK #1059 completion and public timing-claim promotion`.
- **Archive:** `chatgpt_todo/archive/2026-08-11T215500Z_ARU-TIMING-CFD-COMPONENT-CROSSING-BINDING-001.md`.
- **Validation gate:** exact final-head protected CI must pass before #1250 may be marked ready or merged. No beam-data or detector-performance PASS is implied by synthetic/unit closure.
- **Next highest-value child:** `ARU-TIMING-CFD-PEAK-SELECTION-IDENTIFIABILITY-001` — the current 5%-of-global rule is an uncalibrated local-peak selector and not a true prominence measure; test its noise, overlap, saturation and domain-shift behavior before authorising prompt-component timing.
- **Other children:** `ARU-TIMING-CFD-OVERLAP-BASIN-001`, `ARU-TIMING-CFD-REALDATA-TRANSITION-001`, `ARU-TIMING-CFD-TRUTH-TRANSFER-001`, and downstream result regeneration for any study that consumed the changed estimator.
- **Scientific boundary:** no immutable beam waveform population, production Geant4/MC, timing resolution, pile-up mechanism, WLS response, PID metric, rate, ESS, p-value or detector-performance claim was regenerated or promoted by this atom.
- **Status:** `COMPONENT_CROSSING_REPAIR_IMPLEMENTED / EXACT_HEAD_CI_PENDING / #1059_OPEN / DETECTOR_TIMING_CLAIMS_GATED`.
