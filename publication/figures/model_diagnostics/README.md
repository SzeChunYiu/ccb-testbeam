# Model / method diagnostics

Simulation-only and method-closure plots live here. Their captions must preserve evidence classes such as `MC_METHOD_CLOSURE`, `SIMULATION_RESULT`, or `MODEL_DEPENDENT` and must never be described as beam-data detector performance.
