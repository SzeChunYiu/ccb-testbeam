# Active Task

- **Task ID:** `ARU-MC-G4-LOADER-INITIAL-CWD-EXEC-BOUNDARY-001` / issue #1214.
- **Status:** `TRIAGED / NEXT_ACTIVE`; parent `ARU-MC-G4-LOADER-INITIAL-CWD-001` remains `PARTIAL` because this material child is unresolved.
- **Protected main:** `859903ada4a856c998b2bc79298cd4a26c2cb447`, squash merge of #1215.
- **Validated bounded parent now on main:** `ccb_geant4_loader_cwd_attestation_v1` binds a stable current `/proc/<pid>/cwd` directory-object observation to exact runtime+argv process identity. Exact-head push run `31489962787` and PR run `31489967793` both completed successfully on head `5f251aa10aabaddfadcee7f7f9e77b021ce98998`; curated ruff, full non-integration pytest, diagnostics upload and enforcement all passed in both jobs.
- **Validation child closed locally and in CI:** the first live post-exec `chdir` fixture had a timing/readiness defect (`1 failed, 8 passed in 11.92 s` on a later local rerun). Commit `198cd5062982947d12410b9371d43ddaa596c4f0` replaced elapsed-time inference with a test-owned readiness marker emitted only after successful `chdir`; production attestor unchanged. Six consecutive repaired focused runs returned `9 passed`, and exact-head repository CI is green.
- **Scientific reason #1214 is next:** `geant4/setup_and_run.sh` invokes `./hibeam_g4 -c krakow.config -m run_krakow.mac output_krakow.root`. A later current cwd is insufficient to prove the cwd at the actual `execve` boundary because target or wrapper code can call `chdir`/`fchdir` before or after exec.
- **Required #1214 contract:** bind `CWD_exec` to the exact process/executable chain at the exec transition, distinguish wrapper pre-exec cwd changes from post-exec target cwd changes, preserve directory-object identity independently of lexical spelling, and compose with runtime/argv receipts. Do not treat parent-shell cwd or delayed procfs cwd as equivalent evidence.
- **Surviving mechanism candidates:** a minimal launcher that opens `.` and records directory identity immediately before direct exec; a kernel tracing mechanism that captures cwd state at exec; or an equivalent process-identifiable exec-boundary proof. Wrapper/dynamic-loader chains must be explicit rather than collapsed.
- **Negative controls:** wrapper `chdir` before exec; target `chdir` and `fchdir` after exec; PID/starttime reuse; executable mismatch; directory rename/unlink; explicit dynamic-loader launch; namespace/root mismatch.
- **Downstream children after #1214:** `ARU-MC-G4-LOADER-FS-NAMESPACE-001`, `ARU-MC-G4-RELATIVE-INPUT-CONSUMPTION-001`, `ARU-MC-G4-OUTPUT-PATH-CREATION-001`, plus loader cache/config, token/hwcaps, preload/audit, linker/static-input, late-dlopen, wrapper/descendant identity, immutable consumption, runtime manifest, compiled source/stopping controls, event weights and detector response.
- **Claim governance:** `docs/validation/CL-021_scattering_model.md` remains `OPEN — hypothesis FALSIFIED; source model remains GATED`. No detector or MC physics claim is promoted by the cwd work.
- **Four-role handoff:** runtime/physics lead `BLOCK historical initial-cwd provenance pending #1214`; adversarial Linux/filesystem reviewer `ACCEPT bounded current-cwd primitive / BLOCK delayed-procfs substitution`; independent validation reviewer `ACCEPT merged deterministic software oracle / BLOCK HIBEAM/physics inference`; claims/provenance reviewer `BLOCK CL-021 promotion`.
