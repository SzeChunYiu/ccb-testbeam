# Active Task

- **Task ID:** `ARU-MC-G4-RUNTIME-LINK-COOBSERVATION-001`
- **Owner:** hourly Atomic Research Universe audit session
- **Protected main inspected:** `a9b7184bce1b898a2b36143ed4bd7f725d5a0f8a`.
- **Validated predecessor:** PR #1207 / `ARU-MC-G4-MAPPED-PAGE-CONTENT-001` passed exact-head MC Validation run `31464431085` with curated ruff clean and `1537 passed, 1 skipped, 8 xfailed, 1 xpassed`, then squash-merged to protected main as `a9b7184bce1b898a2b36143ed4bd7f725d5a0f8a`. Merged #1206 separately binds static ELF link declarations to #1204 runtime file identities.
- **Selected universe:** remove the remaining pathname-reopen gap between mapped-object identity and ELF metadata. For each file-backed executable object keyed by `(dev_major,dev_minor,inode)`, open a current path resolving to that exact mapped inode, derive bytes, SHA-256 and ELF metadata from the same opened descriptor, and require stable path resolution, process start-time and complete executable mapping projection before/after.
- **Core invariant:** `K_stat(path,t0) = K_fstat(fd) = K_map = K_stat(path,t1)` and, from one descriptor snapshot `B`, both `H=SHA256(B)` and `E=parse_ELF(B)` are derived. Non-path `DT_NEEDED=d` requires exactly one co-observed object with `DT_SONAME=d`; relative slash-containing dependencies remain blocked pending cwd provenance; absolute `DT_NEEDED`/`PT_INTERP` close by stable resolved device/inode identity to one co-observed object.
- **Mechanisms considered:** independent later pathname reopen (rejected for this atom); same-open-descriptor hash+ELF snapshot (survives); mandatory `/proc/<pid>/map_files` FD (stronger but capability-blocked/not claimed); basename-family matching (rejected); malformed ELF-magic downgraded to non-ELF (rejected).
- **Implementation:** draft PR #1208 on `audit/geant4-runtime-link-coobservation`. Tool commit `1ebebdf3fa2a60642fdbeb84fd6e5c73abdd7ccd`; hostile tests `fc821619cf72972a81302609e8f8a560c8d48c52`; curated-rufﬀ inclusion `4f637fabe507c040ab5421f1fc63dcf191391abd`; immutable ARU record `e60cad31a191bad21737d204267bb37bf9ba14a6`.
- **Executed local evidence:** Python 3.13.5, Linux 6.18.35 x86_64, glibc 2.41, no RNG. `python -m py_compile` passed for the new tool/test. A focused pytest attempt in a deliberately partial `/mnt/data/coobs` tree stopped during collection because predecessor audit modules were absent from that partial tree; this is recorded as an environment/fixture-assembly failure, not a repository test result. Local `ruff` is unavailable, so no local ruff PASS is claimed.
- **Committed hostile matrix:** symlinked interpreter; absolute dependency symlink; relative dependency cwd blocker; duplicate SONAME ambiguity; malformed ELF-magic fail-closed; injected pathname replacement while old FD remains open; mapping projection drift; wrong parent final-build receipt.
- **CI/merge gate:** fresh exact-head GitHub MC Validation is required after the final coordination commit. Do not mark ready or merge #1208 until that exact head has ruff and full non-integration pytest success and base ancestry remains valid.
- **Four-role state:** runtime-provenance lead `ACCEPT bounded mechanism / BLOCK HIBEAM authorisation`; adversarial reviewer `ACCEPT same-descriptor/inode-resolution contract / BLOCK historical-loader inference`; independent validation reviewer `BLOCK merge pending exact-head CI`; claims/provenance reviewer `BLOCK CL-021 and detector promotion`.
- **Parent dependency:** #1182 / compiled Geant4 provenance. #1178, #1179, #1058, #1053/#880 and CL-021 remain gated.
- **Child universes:** `ARU-MC-G4-LOADER-SEARCH-001`, `ARU-MC-G4-LINK-COMMAND-001`, `ARU-MC-G4-LATE-DLOPEN-001`, `ARU-MC-G4-NONEXEC-RELOCATION-001`, plus wrapper/descendant, immutable-consumption, runtime-manifest, compiled source/stopping controls, event-weight and detector-response children.
- **Status:** `ACTIVE / DRAFT_PR_1208 / SAME_FD_IMPLEMENTATION_COMMITTED / EXACT_HEAD_CI_PENDING / REAL_HIBEAM_RUNTIME_BLOCKED / DETECTOR_INFERENCE_BLOCKED`
